import { Question } from '../types';

export const appServiceQuestions: Question[] = [

  // ──────────────────────────────────────────────────────────────────────────
  // PROVISION AN APP SERVICE PLAN (cp-161 to cp-170)
  // ──────────────────────────────────────────────────────────────────────────

  {
    type: 'single-choice',
    id: 'cp-161',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'A company needs to host a .NET web application on Azure App Service. The application must support custom domains, SSL certificates, and up to 10 deployment slots. Which App Service plan tier should you choose?',
    options: [
      { id: 'a', text: 'Free (F1)' },
      { id: 'b', text: 'Shared (D1)' },
      { id: 'c', text: 'Standard (S1)' },
      { id: 'd', text: 'Premium (P1v3)' },
    ],
    correctOptionId: 'd',
    explanation: 'The Premium tier supports up to 20 deployment slots, custom domains, and SSL certificates. Standard supports only 5 deployment slots, which is insufficient for 10 slots. Free and Shared tiers do not support custom domains with SSL or deployment slots.',
  },

  {
    type: 'single-choice',
    id: 'cp-162',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'You are provisioning an App Service plan in the East US region. You need the plan to support always-on functionality so that your application does not go idle. Which is the minimum tier that satisfies this requirement?',
    options: [
      { id: 'a', text: 'Free (F1)' },
      { id: 'b', text: 'Shared (D1)' },
      { id: 'c', text: 'Basic (B1)' },
      { id: 'd', text: 'Standard (S1)' },
    ],
    correctOptionId: 'c',
    explanation: 'The Always On feature is available starting from the Basic tier (B1) and above. Free and Shared tiers do not support Always On, meaning applications on those tiers may be unloaded after a period of inactivity.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-163',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'You are planning an App Service plan for a production workload. Which two features are ONLY available on the Premium V3 or Isolated tier and NOT on the Standard tier? (Choose two.)',
    options: [
      { id: 'a', text: 'Custom domains and SSL' },
      { id: 'b', text: 'Zone redundancy (availability zone support)' },
      { id: 'c', text: 'Auto scaling (manual scale out)' },
      { id: 'd', text: 'Private Endpoints support via App Service Environment' },
      { id: 'e', text: 'Up to 3 deployment slots' },
    ],
    correctOptionIds: ['b', 'd'],
    requiredSelections: 2,
    explanation: 'Zone redundancy requires Premium V3 or Isolated (ASE) tier. App Service Environment (which provides full VNet isolation and Private Endpoints natively) is an Isolated tier feature. Custom domains, SSL, manual scale-out, and a limited number of deployment slots are available on Standard tier.',
  },

  {
    type: 'yes-no',
    id: 'cp-164',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    scenario: 'Your organization is provisioning App Service plans for different teams. Review each statement and determine whether it is accurate.',
    statements: [
      { id: 's1', text: 'Multiple App Service apps can share the same App Service plan, sharing the same compute resources.', correct: true },
      { id: 's2', text: 'An App Service plan can span multiple Azure regions to provide geo-redundancy.', correct: false },
      { id: 's3', text: 'Changing an App Service plan from Standard to Premium requires deleting and recreating the plan.', correct: false },
    ],
    explanation: 'Multiple apps can indeed share one App Service plan and its underlying VMs. An App Service plan is scoped to a single region — it cannot span multiple regions. You can scale up (change tier) an App Service plan in-place without recreation; Azure supports changing the pricing tier through the Azure portal or CLI.',
  },

  {
    type: 'dropdown',
    id: 'cp-165',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'Complete the following statement about App Service plan operating systems.',
    segments: [
      { type: 'text', text: 'When you create an App Service plan, you must choose an operating system. An App Service plan configured for ' },
      { type: 'dropdown', id: 'dd1', options: ['Windows', 'Linux', 'Both Windows and Linux'], correctOption: 'Linux' },
      { type: 'text', text: ' cannot host Windows-based web apps, and a plan configured for ' },
      { type: 'dropdown', id: 'dd2', options: ['Windows', 'Linux', 'Either OS'], correctOption: 'Windows' },
      { type: 'text', text: ' cannot host Linux-based containers.' },
    ],
    explanation: 'App Service plans are OS-specific. A Linux plan can only host Linux-based apps (including custom containers and code-based Linux apps). A Windows plan can only host Windows-based apps. You cannot mix operating systems within a single App Service plan.',
  },

  {
    type: 'single-choice',
    id: 'cp-166',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'Northwind Traders requires an App Service plan for a highly regulated workload. The application must run on dedicated hardware, be fully isolated from other customers, and support direct VNet integration at the plan level. Which App Service plan type should you provision?',
    options: [
      { id: 'a', text: 'Premium V3 (P3v3)' },
      { id: 'b', text: 'Isolated V2 (I2v2) using App Service Environment v3' },
      { id: 'c', text: 'Standard S3' },
      { id: 'd', text: 'Premium P1v2 with VNet Integration enabled' },
    ],
    correctOptionId: 'b',
    explanation: 'The Isolated tier (App Service Environment v3) provides full hardware isolation on dedicated infrastructure within your own VNet. It is specifically designed for highly regulated, high-scale workloads. Premium tier offers VNet Integration but still shares underlying infrastructure with other customers.',
  },

  {
    type: 'single-choice',
    id: 'cp-167',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'You need to provision an App Service plan using the Azure CLI. Which command creates a Premium V3 plan named "MyPlan" in resource group "MyRG" in East US with 2 workers?',
    options: [
      { id: 'a', text: 'az appservice plan create --name MyPlan --resource-group MyRG --location eastus --sku P1V3 --number-of-workers 2' },
      { id: 'b', text: 'az webapp plan create --name MyPlan --resource-group MyRG --location eastus --sku P1V3 --workers 2' },
      { id: 'c', text: 'az appservice plan create --name MyPlan --resource-group MyRG --sku Premium --workers 2' },
      { id: 'd', text: 'az webapp create --plan MyPlan --resource-group MyRG --location eastus --sku P1V3' },
    ],
    correctOptionId: 'a',
    explanation: 'The correct command is "az appservice plan create" with --name, --resource-group, --location, --sku (using the SKU code like P1V3), and --number-of-workers parameters. The "az webapp plan create" and "az webapp create" commands do not exist for plan provisioning.',
  },

  {
    type: 'drag-drop',
    id: 'cp-168',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'Match each App Service plan tier with its maximum number of deployment slots.',
    items: [
      { id: 'item1', text: '0 deployment slots' },
      { id: 'item2', text: '5 deployment slots' },
      { id: 'item3', text: '20 deployment slots' },
      { id: 'item4', text: '1 deployment slot (staging only)' },
    ],
    targets: [
      { id: 'target1', label: 'Free / Shared tier', correctItemId: 'item1' },
      { id: 'target2', label: 'Basic tier', correctItemId: 'item4' },
      { id: 'target3', label: 'Standard tier', correctItemId: 'item2' },
      { id: 'target4', label: 'Premium tier', correctItemId: 'item3' },
    ],
    explanation: 'Free and Shared tiers do not support deployment slots. Basic tier supports 1 slot (a staging slot). Standard tier supports up to 5 slots. Premium tier supports up to 20 slots. The Isolated tier also supports up to 20 slots.',
  },

  {
    type: 'single-choice',
    id: 'cp-169',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'You have an App Service plan with three web apps. The plan is currently at the Basic B2 tier with 2 instances. You need to scale up to Standard S2. What happens to the web apps hosted on the plan when you change the tier?',
    options: [
      { id: 'a', text: 'All web apps are deleted and must be redeployed to the new plan.' },
      { id: 'b', text: 'The web apps are automatically migrated to the new tier with no downtime interruption; they continue to run on the plan.' },
      { id: 'c', text: 'Each web app must be manually moved to a new Standard plan.' },
      { id: 'd', text: 'The web apps are stopped while the scale-up operation completes, then restarted.' },
    ],
    correctOptionId: 'b',
    explanation: 'When you scale up (change the pricing tier) of an App Service plan, all apps in the plan move to the new tier automatically. Azure performs the transition without requiring manual migration of individual apps. There may be a brief restart of the apps during the transition, but there is no data loss or requirement to redeploy.',
  },

  {
    type: 'single-choice',
    id: 'cp-170',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Provision an App Service plan',
    stem: 'A developer team wants to run multiple low-traffic apps to minimize cost. The apps need custom domain support but no SSL and no scale-out. What is the most cost-effective App Service plan tier?',
    options: [
      { id: 'a', text: 'Free (F1) — no charge, supports custom domains' },
      { id: 'b', text: 'Shared (D1) — low cost, supports custom domains' },
      { id: 'c', text: 'Basic (B1) — supports custom domains and SSL' },
      { id: 'd', text: 'Standard (S1) — required for custom domains' },
    ],
    correctOptionId: 'b',
    explanation: 'The Shared (D1) tier supports custom domains and is more cost-effective than Basic. Free tier does NOT support custom domains. Basic supports custom domains and SSL, but since SSL is not required, Shared is the minimum cost tier with custom domain support.',
  },

  // ──────────────────────────────────────────────────────────────────────────
  // CONFIGURE SCALING FOR AN APP SERVICE PLAN (cp-171 to cp-180)
  // ──────────────────────────────────────────────────────────────────────────

  {
    type: 'single-choice',
    id: 'cp-171',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'You have an App Service plan on the Standard S2 tier. You want to automatically increase the instance count when CPU utilization exceeds 75% for 10 minutes and decrease it when CPU drops below 30%. What feature should you configure?',
    options: [
      { id: 'a', text: 'Manual scale-out by adjusting the instance count slider' },
      { id: 'b', text: 'Azure Autoscale with scale-out and scale-in rules based on CPU metric' },
      { id: 'c', text: 'Azure Load Balancer health probes' },
      { id: 'd', text: 'Azure Monitor alert rules with action groups' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Autoscale allows you to define rules based on metrics (such as CPU percentage) with configurable thresholds and time windows. You can configure scale-out rules (increase instances) and scale-in rules (decrease instances). Manual scale-out requires human intervention. Load Balancer health probes and Monitor alerts do not directly control instance scaling.',
  },

  {
    type: 'dropdown',
    id: 'cp-172',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'Complete the following statement about App Service plan scaling limits.',
    segments: [
      { type: 'text', text: 'The maximum number of instances you can scale out to on a Standard App Service plan is ' },
      { type: 'dropdown', id: 'dd1', options: ['3', '10', '30', '100'], correctOption: '10' },
      { type: 'text', text: ', while on a Premium V3 plan the maximum is ' },
      { type: 'dropdown', id: 'dd2', options: ['10', '20', '30', '100'], correctOption: '30' },
      { type: 'text', text: ' instances.' },
    ],
    explanation: 'Standard App Service plans support up to 10 instances with autoscale. Premium V3 plans support up to 30 instances. Isolated (ASE) plans can scale to 100 instances. These limits reflect the hardware capacity available per tier.',
  },

  {
    type: 'single-choice',
    id: 'cp-173',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'You configure autoscale for an App Service plan with a minimum of 2 instances, a maximum of 8, and a default of 2. A scale-out rule triggers when CPU > 70% for 5 minutes. After a scale-out event adds 2 instances, how long must pass before another scale-out rule can trigger by default?',
    options: [
      { id: 'a', text: '1 minute' },
      { id: 'b', text: '5 minutes' },
      { id: 'c', text: '10 minutes (cool-down period)' },
      { id: 'd', text: '30 minutes' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Autoscale has a cool-down period to prevent rapid flapping. After a scale action completes, the default cool-down period for App Service is 5 minutes. During this time, no additional scaling actions will occur regardless of the metric values. The cool-down period is configurable.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-174',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'You are configuring autoscale for an App Service plan. Which two metrics can be used to trigger autoscale rules? (Choose two.)',
    options: [
      { id: 'a', text: 'CPU Percentage of the App Service plan' },
      { id: 'b', text: 'HTTP Queue Length' },
      { id: 'c', text: 'Number of Azure AD sign-ins per minute' },
      { id: 'd', text: 'Disk IOPS on the underlying VM' },
      { id: 'e', text: 'Azure Blob Storage capacity' },
    ],
    correctOptionIds: ['a', 'b'],
    requiredSelections: 2,
    explanation: 'App Service autoscale supports metrics such as CPU Percentage, Memory Percentage, Disk Queue Length, HTTP Queue Length, and Data In/Out. CPU Percentage and HTTP Queue Length are both valid metrics for App Service autoscale rules. Azure AD sign-in count, raw disk IOPS, and Blob Storage metrics are not available as App Service plan autoscale metrics.',
  },

  {
    type: 'yes-no',
    id: 'cp-175',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'Your team is discussing App Service scaling options. Evaluate each statement.',
    statements: [
      { id: 's1', text: 'Scale-up (changing from B1 to S1) increases the CPU and RAM of each instance without changing the number of instances.', correct: true },
      { id: 's2', text: 'Scale-out on a Free tier plan allows you to run up to 3 instances.', correct: false },
      { id: 's3', text: 'Autoscale can be configured to scale based on a specific schedule, such as adding instances every weekday at 8 AM.', correct: true },
    ],
    explanation: 'Scale-up changes the size (CPU/RAM) of individual instances, not the count. Free tier does not support scale-out — it runs on shared infrastructure with no instance control. Autoscale supports schedule-based scaling profiles in addition to metric-based rules, allowing you to preemptively scale before predictable load increases.',
  },

  {
    type: 'drag-drop',
    id: 'cp-176',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'Match each scaling scenario to the correct scaling action type for an App Service plan.',
    items: [
      { id: 'item1', text: 'Increase instance count from 2 to 5 based on CPU metric' },
      { id: 'item2', text: 'Change the plan from Standard S1 to Premium P1v3' },
      { id: 'item3', text: 'Reduce instance count from 5 to 2 at 6 PM daily' },
      { id: 'item4', text: 'Manually set instance count to 4 without automation' },
    ],
    targets: [
      { id: 'target1', label: 'Metric-based scale-out', correctItemId: 'item1' },
      { id: 'target2', label: 'Scale-up (vertical scaling)', correctItemId: 'item2' },
      { id: 'target3', label: 'Schedule-based scale-in', correctItemId: 'item3' },
      { id: 'target4', label: 'Manual scale', correctItemId: 'item4' },
    ],
    explanation: 'Scale-out/in (horizontal scaling) changes the number of instances. Scale-up/down (vertical scaling) changes the size of instances by changing the plan tier. Schedule-based scaling uses time triggers. Manual scaling requires user action each time. Understanding these distinctions is key for the AZ-104 exam.',
  },

  {
    type: 'single-choice',
    id: 'cp-177',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'You have configured autoscale on a Standard S2 App Service plan. The minimum instances is set to 1 and maximum to 10. During a maintenance window, you need to temporarily pin the app to exactly 3 instances and prevent autoscale from changing the count. What should you do?',
    options: [
      { id: 'a', text: 'Delete the autoscale setting and set manual scale to 3 instances.' },
      { id: 'b', text: 'Set both the minimum and maximum instance count to 3 in the autoscale setting.' },
      { id: 'c', text: 'Disable the autoscale setting and manually set the instance count to 3.' },
      { id: 'd', text: 'Add a schedule-based profile that sets minimum=3 and maximum=3 during the maintenance window.' },
    ],
    correctOptionId: 'd',
    explanation: 'The cleanest approach for a temporary pin during a maintenance window is to add a time-limited autoscale profile with both minimum and maximum set to 3. This prevents scaling actions during the window without permanently disabling autoscale. You could also disable autoscale and set manual to 3, but a schedule-based profile is the operational best practice for temporary constraints.',
  },

  {
    type: 'single-choice',
    id: 'cp-178',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'An App Service plan is on the Basic B1 tier. A developer wants to configure autoscale so that the app automatically scales out during peak hours. What must the developer do first?',
    options: [
      { id: 'a', text: 'Enable the Always On setting for the web app.' },
      { id: 'b', text: 'Upgrade the App Service plan to Standard tier or higher.' },
      { id: 'c', text: 'Enable Diagnostic Logs for the App Service plan.' },
      { id: 'd', text: 'Configure Azure Monitor action groups for the plan.' },
    ],
    correctOptionId: 'b',
    explanation: 'Autoscale (automatic scale-out based on metrics or schedules) is only available on the Standard tier and above. The Basic tier supports manual scale-out (up to 3 instances) but does not support autoscale rules. The developer must upgrade to Standard S1 or higher before configuring autoscale.',
  },

  {
    type: 'single-choice',
    id: 'cp-179',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'You need to use Azure CLI to configure autoscale on an App Service plan to scale out by 1 instance when average CPU exceeds 80% over 10 minutes. Which command creates this autoscale rule?',
    options: [
      { id: 'a', text: 'az monitor autoscale rule create --autoscale-name MyScale --condition "Percentage CPU > 80 avg 10m" --scale out 1' },
      { id: 'b', text: 'az webapp scale --name MyApp --resource-group MyRG --cpu-threshold 80 --instances 1' },
      { id: 'c', text: 'az monitor autoscale create --resource MyPlanId --min-count 1 --max-count 5 --count 1' },
      { id: 'd', text: 'az appservice plan update --name MyPlan --resource-group MyRG --number-of-workers 5' },
    ],
    correctOptionId: 'a',
    explanation: 'The "az monitor autoscale rule create" command creates autoscale rules for App Service plans. It requires specifying the autoscale setting name and a condition expression including the metric, operator, threshold, and aggregation window. The other options are either incorrect commands or configure different things (az appservice plan update sets a static worker count).',
  },

  {
    type: 'single-choice',
    id: 'cp-180',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure scaling for an App Service plan',
    stem: 'Your App Service plan hosts a web app that processes background jobs. You notice that the HTTP Queue Length metric stays at 0 but CPU frequently spikes to 90%. You have a scale-out rule triggered by HTTP Queue Length > 10 that never fires. What should you change?',
    options: [
      { id: 'a', text: 'Change the scale-out trigger metric to CPU Percentage > 70%.' },
      { id: 'b', text: 'Increase the HTTP Queue Length threshold to 50.' },
      { id: 'c', text: 'Add an additional web app to the same App Service plan.' },
      { id: 'd', text: 'Enable the Always On setting for the web app.' },
    ],
    correctOptionId: 'a',
    explanation: 'Since the workload is CPU-intensive (background jobs that do not queue HTTP requests), HTTP Queue Length is not an appropriate metric — it stays at 0. The correct approach is to use CPU Percentage as the scale-out metric. CPU Percentage accurately reflects the compute load from background processing and will trigger scaling when it exceeds the threshold.',
  },

  // ──────────────────────────────────────────────────────────────────────────
  // CREATE AN APP SERVICE (cp-181 to cp-190)
  // ──────────────────────────────────────────────────────────────────────────

  {
    type: 'single-choice',
    id: 'cp-181',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'You need to create an Azure Web App that runs a Python 3.11 application on Linux. Which Azure CLI command correctly creates the web app named "MyPythonApp" in resource group "WebRG" using an existing plan "MyLinuxPlan"?',
    options: [
      { id: 'a', text: 'az webapp create --name MyPythonApp --resource-group WebRG --plan MyLinuxPlan --runtime "PYTHON:3.11"' },
      { id: 'b', text: 'az webapp create --name MyPythonApp --resource-group WebRG --plan MyLinuxPlan --os-type linux --language python3.11' },
      { id: 'c', text: 'az appservice webapp new --name MyPythonApp --resource-group WebRG --plan MyLinuxPlan --stack python --version 3.11' },
      { id: 'd', text: 'az webapp new --name MyPythonApp --resource-group WebRG --sku P1V3 --runtime PYTHON:3.11' },
    ],
    correctOptionId: 'a',
    explanation: 'The correct command is "az webapp create" with --name, --resource-group, --plan, and --runtime parameters. The runtime string for Python 3.11 on Linux is "PYTHON:3.11". The other options use invalid parameters (--language, --stack/--version are not valid for az webapp create) or wrong commands.',
  },

  {
    type: 'single-choice',
    id: 'cp-182',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'You are creating an Azure Web App. After creation, you need to configure it so that HTTP requests are automatically redirected to HTTPS. Where in the Azure portal do you configure this setting?',
    options: [
      { id: 'a', text: 'App Service > Networking > TLS/SSL settings > HTTPS Only = On' },
      { id: 'b', text: 'App Service > Configuration > General settings > HTTPS Only = On' },
      { id: 'c', text: 'App Service > Custom domains > HTTPS redirect = Enabled' },
      { id: 'd', text: 'App Service > TLS/SSL settings > Minimum TLS Version = 1.2' },
    ],
    correctOptionId: 'b',
    explanation: 'The HTTPS Only setting is located in App Service > Configuration > General settings. When enabled, all HTTP traffic is redirected to HTTPS with a 301 redirect. This is separate from the minimum TLS version setting (also in TLS/SSL settings) and the custom domain configuration.',
  },

  {
    type: 'yes-no',
    id: 'cp-183',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'A development team is creating App Services in Azure. Evaluate each statement about App Service creation and configuration.',
    statements: [
      { id: 's1', text: 'When you create an App Service (web app), you can choose to create a new App Service plan or use an existing one in the same region and OS type.', correct: true },
      { id: 's2', text: 'An App Service web app name must be globally unique across all Azure subscriptions because it forms part of the default azurewebsites.net hostname.', correct: true },
      { id: 's3', text: 'You can change the runtime stack (e.g., from .NET 6 to .NET 8) of an App Service without stopping or restarting the application.', correct: false },
    ],
    explanation: 'App Service plans are shared within the same region and OS type. The app name must be globally unique as it becomes {name}.azurewebsites.net. Changing the runtime stack requires the app to restart — Azure will stop and restart the application to apply the new runtime configuration.',
  },

  {
    type: 'single-choice',
    id: 'cp-184',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'You need to create a web app that uses a Docker container image stored in Azure Container Registry (ACR). The ACR is named "myregistry" and the image is "myapp:latest". During web app creation, which configuration is needed to enable the app to pull from ACR?',
    options: [
      { id: 'a', text: 'Enable the web app\'s system-assigned managed identity and grant it AcrPull role on the ACR.' },
      { id: 'b', text: 'Set the container registry login server to myregistry.azurecr.io with admin credentials.' },
      { id: 'c', text: 'Both A and B are valid approaches; managed identity is preferred for security.' },
      { id: 'd', text: 'Configure a service endpoint between the App Service VNet and ACR.' },
    ],
    correctOptionId: 'c',
    explanation: 'Azure App Service supports two methods for authenticating to ACR: using admin credentials (username/password) or using a managed identity with AcrPull role assignment. Both methods work, but using a managed identity with RBAC is the recommended security best practice as it avoids storing credentials. Service endpoints alone do not grant authentication.',
  },

  {
    type: 'drag-drop',
    id: 'cp-185',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'Match each App Service application type to its correct runtime or hosting model.',
    items: [
      { id: 'item1', text: 'Code-based deployment with Node.js 20 runtime' },
      { id: 'item2', text: 'Custom Docker container from a private registry' },
      { id: 'item3', text: 'Static HTML/CSS/JavaScript files without server-side processing' },
      { id: 'item4', text: 'Multi-container application defined in a Docker Compose file' },
    ],
    targets: [
      { id: 'target1', label: 'Web App (Linux, Code)', correctItemId: 'item1' },
      { id: 'target2', label: 'Web App for Containers (single container)', correctItemId: 'item2' },
      { id: 'target3', label: 'Azure Static Web Apps', correctItemId: 'item3' },
      { id: 'target4', label: 'Web App for Containers (Docker Compose)', correctItemId: 'item4' },
    ],
    explanation: 'Linux App Service supports code-based deployments with built-in runtime stacks like Node.js. Web App for Containers supports single Docker containers from any registry. Azure Static Web Apps is optimized for static sites (not standard App Service). Multi-container deployments on App Service use Docker Compose configuration in the Web App for Containers model.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-186',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'You are creating an App Service and need to configure application settings. Which two statements about App Service application settings are correct? (Choose two.)',
    options: [
      { id: 'a', text: 'Application settings are exposed to the application as environment variables.' },
      { id: 'b', text: 'Application settings in App Service override values in the application\'s web.config or appsettings.json.' },
      { id: 'c', text: 'Connection strings and application settings are both encrypted at rest and visible in plain text only through the Azure portal.' },
      { id: 'd', text: 'Application settings can reference Azure Key Vault secrets using a Key Vault reference syntax.' },
      { id: 'e', text: 'Application settings are automatically synchronized to all deployment slots without sticky slot configuration.' },
    ],
    correctOptionIds: ['a', 'd'],
    requiredSelections: 2,
    explanation: 'App Service application settings are injected as environment variables at runtime, making them available to the app. You can also use Key Vault references (format: @Microsoft.KeyVault(SecretUri=...)) to securely reference secrets. Application settings do NOT automatically override config files in all cases (depends on the framework). By default, settings are shared across slots unless marked as slot settings (sticky). Both app settings and connection strings are encrypted at rest.',
  },

  {
    type: 'single-choice',
    id: 'cp-187',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'You create an App Service web app and need to set a connection string for a SQL Database. You want to ensure this connection string is slot-specific and does not get swapped when you perform a deployment slot swap. What should you configure?',
    options: [
      { id: 'a', text: 'Add the connection string in the App Service Configuration and mark it as a "Deployment slot setting" (sticky to slot).' },
      { id: 'b', text: 'Store the connection string in Azure Key Vault and reference it as an application setting.' },
      { id: 'c', text: 'Add the connection string directly in the app\'s web.config file and deploy the file.' },
      { id: 'd', text: 'Create a separate App Service plan for each deployment slot with its own connection string.' },
    ],
    correctOptionId: 'a',
    explanation: 'To prevent a configuration value from being swapped during slot swaps, you mark it as a "Deployment slot setting" (also called a sticky setting) in the App Service Configuration. This ensures the production slot always uses the production connection string and the staging slot uses the staging connection string, regardless of code swaps.',
  },

  {
    type: 'dropdown',
    id: 'cp-188',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'Complete the statement about App Service logging configuration.',
    segments: [
      { type: 'text', text: 'To enable application logging to Azure Blob Storage for a Linux App Service, you must configure ' },
      { type: 'dropdown', id: 'dd1', options: ['Diagnostic logs in App Service', 'Azure Monitor Diagnostic Settings', 'Log Analytics workspace directly'], correctOption: 'Azure Monitor Diagnostic Settings' },
      { type: 'text', text: '. For Windows App Service, application logging to the filesystem is configured via ' },
      { type: 'dropdown', id: 'dd2', options: ['Azure Monitor', 'App Service Diagnostic logs blade', 'Azure Storage Explorer'], correctOption: 'App Service Diagnostic logs blade' },
      { type: 'text', text: '.' },
    ],
    explanation: 'For Linux App Service, application logs (stdout/stderr) are sent to a storage container via Azure Monitor Diagnostic Settings. For Windows App Service, the App Service Diagnostic logs blade provides options for Application Logging (Filesystem), Application Logging (Blob), Web server logging, and more. The Linux and Windows logging configuration paths differ in the portal.',
  },

  {
    type: 'single-choice',
    id: 'cp-189',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'After creating a web app, you need to access the Kudu console for advanced file management and debugging. Which URL pattern provides access to the Kudu console for a web app named "ContosoApp"?',
    options: [
      { id: 'a', text: 'https://contosoapp.azurewebsites.net/kudu' },
      { id: 'b', text: 'https://contosoapp.scm.azurewebsites.net' },
      { id: 'c', text: 'https://portal.azure.com/kudu/contosoapp' },
      { id: 'd', text: 'https://contosoapp.management.azure.com/kudu' },
    ],
    correctOptionId: 'b',
    explanation: 'The Kudu console (App Service advanced tools) is accessible at https://{appname}.scm.azurewebsites.net. The .scm subdomain provides access to the Source Control Manager (SCM) endpoint, which hosts Kudu. From there you can access the debug console, environment variables, process explorer, log streaming, and deployment logs.',
  },

  {
    type: 'single-choice',
    id: 'cp-190',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Create an App Service',
    stem: 'You need to create a web app that must respond to requests on both Windows and Linux. You want to deploy the same application code to both. What is a limitation you must consider?',
    options: [
      { id: 'a', text: 'A single App Service plan can host both Windows and Linux apps simultaneously.' },
      { id: 'b', text: 'Each App Service plan is OS-specific; you need separate plans for Windows and Linux apps.' },
      { id: 'c', text: 'Linux App Service does not support custom domains.' },
      { id: 'd', text: 'Windows App Service does not support container-based deployments.' },
    ],
    correctOptionId: 'b',
    explanation: 'A fundamental constraint of App Service plans is that they are OS-specific — a plan is either Windows or Linux, not both. To host apps on both operating systems, you need two separate App Service plans (one Windows, one Linux). Windows App Service does support containers via Windows Containers. Linux App Service fully supports custom domains.',
  },

  // ──────────────────────────────────────────────────────────────────────────
  // CONFIGURE CERTIFICATES AND TLS FOR AN APP SERVICE (cp-191 to cp-200)
  // ──────────────────────────────────────────────────────────────────────────

  {
    type: 'single-choice',
    id: 'cp-191',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'You need to bind a custom SSL certificate to a web app that uses a custom domain "app.contoso.com". The certificate is stored in Azure Key Vault. What is the correct sequence of steps?',
    options: [
      { id: 'a', text: '1. Import the certificate from Key Vault to App Service. 2. Add the custom domain. 3. Create an SSL binding for the domain.' },
      { id: 'b', text: '1. Add the custom domain. 2. Import the certificate from Key Vault to App Service. 3. Create an SSL binding for the domain.' },
      { id: 'c', text: '1. Create an SSL binding. 2. Add the custom domain. 3. Import the certificate from Key Vault.' },
      { id: 'd', text: '1. Add the custom domain. 2. Create an SSL binding. 3. Import the certificate from Key Vault.' },
    ],
    correctOptionId: 'b',
    explanation: 'The correct order is: (1) Add and verify the custom domain ownership (via DNS records), then (2) import or upload the certificate to App Service, then (3) create the SSL binding that associates the certificate with the domain. You cannot bind a certificate to a domain that has not yet been added to the web app.',
  },

  {
    type: 'single-choice',
    id: 'cp-192',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'You want to secure your App Service app at no additional certificate cost. Your app uses the default azurewebsites.net domain. Which certificate option should you use?',
    options: [
      { id: 'a', text: 'Purchase an App Service Certificate from Azure.' },
      { id: 'b', text: 'Upload a certificate from a third-party CA.' },
      { id: 'c', text: 'Use the free App Service Managed Certificate.' },
      { id: 'd', text: 'Use a self-signed certificate generated from Kudu.' },
    ],
    correctOptionId: 'c',
    explanation: 'Azure provides a free App Service Managed Certificate for custom domains mapped to an App Service. However, for the default *.azurewebsites.net domain, HTTPS is already provided by Microsoft at no charge without any certificate configuration needed. The free Managed Certificate is for custom domains. App Service Certificates are paid, and self-signed certificates are not trusted by browsers.',
  },

  {
    type: 'dropdown',
    id: 'cp-193',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'Complete the statement about TLS binding types in App Service.',
    segments: [
      { type: 'text', text: 'When binding a certificate to a custom domain in App Service, ' },
      { type: 'dropdown', id: 'dd1', options: ['SNI SSL', 'IP-based SSL', 'Both binding types'], correctOption: 'SNI SSL' },
      { type: 'text', text: ' uses Server Name Indication and does not require a dedicated IP address, while ' },
      { type: 'dropdown', id: 'dd2', options: ['SNI SSL', 'IP-based SSL', 'Wildcard SSL'], correctOption: 'IP-based SSL' },
      { type: 'text', text: ' assigns a dedicated inbound IP and incurs an additional charge.' },
    ],
    explanation: 'SNI SSL uses Server Name Indication (TLS extension) to serve the correct certificate for a domain without a dedicated IP — this is the modern standard and does not cost extra. IP-based SSL assigns a dedicated inbound IP address to the app, which is needed for older clients that do not support SNI, but it incurs an additional charge per binding.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-194',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'You are securing an App Service web app with TLS. Which two certificate types can be uploaded or imported into App Service for use with custom domains? (Choose two.)',
    options: [
      { id: 'a', text: 'A PFX (PKCS#12) certificate with private key and password' },
      { id: 'b', text: 'A PEM-encoded public certificate (no private key) for use in code via WEBSITE_LOAD_CERTIFICATES' },
      { id: 'c', text: 'A DER-encoded certificate with .cer extension for TLS binding' },
      { id: 'd', text: 'A certificate stored in Azure Key Vault' },
      { id: 'e', text: 'A certificate from a public CRL (Certificate Revocation List)' },
    ],
    correctOptionIds: ['a', 'd'],
    requiredSelections: 2,
    explanation: 'App Service accepts PFX certificates (with private key, required for TLS binding) and certificates imported from Azure Key Vault. Public certificates (.cer/.pem without private key) can be uploaded for use in code but cannot be used for TLS binding (no private key). DER-encoded .cer files and CRL objects cannot be used for TLS termination in App Service.',
  },

  {
    type: 'single-choice',
    id: 'cp-195',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'Your company policy requires that all App Service apps enforce a minimum TLS version of 1.2. How do you enforce this across a web app?',
    options: [
      { id: 'a', text: 'Configure the HTTPS Only setting to force TLS 1.2.' },
      { id: 'b', text: 'Set the Minimum TLS Version to 1.2 in the App Service TLS/SSL settings.' },
      { id: 'c', text: 'Set the Minimum TLS Version to 1.2 in the App Service Configuration > General settings.' },
      { id: 'd', text: 'Apply an Azure Policy that enforces TLS 1.2 at the network layer.' },
    ],
    correctOptionId: 'b',
    explanation: 'The Minimum TLS Version setting is found in App Service > TLS/SSL settings (also accessible under Settings in the portal). Setting it to 1.2 rejects any connections using TLS 1.0 or 1.1. HTTPS Only forces HTTP to redirect to HTTPS but does not control the TLS version. General settings do not contain this option.',
  },

  {
    type: 'yes-no',
    id: 'cp-196',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'Your team is configuring certificates for App Service. Evaluate each statement.',
    statements: [
      { id: 's1', text: 'The free App Service Managed Certificate supports wildcard certificates (e.g., *.contoso.com).', correct: false },
      { id: 's2', text: 'An App Service Certificate purchased through Azure is automatically stored in Azure Key Vault.', correct: true },
      { id: 's3', text: 'A certificate uploaded to one web app in a resource group can be shared with another web app in the same resource group.', correct: true },
    ],
    explanation: 'The free App Service Managed Certificate does NOT support wildcard certificates — it only covers a single custom domain and does not support root apex domains either. App Service Certificates purchased via Azure are automatically stored in and renewed from Azure Key Vault. Certificates can be shared across web apps within the same resource group or subscription by importing them from the TLS/SSL settings.',
  },

  {
    type: 'single-choice',
    id: 'cp-197',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'You need your App Service application code to access a client certificate uploaded to the web app. How do you make the certificate\'s thumbprint available to the app?',
    options: [
      { id: 'a', text: 'Set the WEBSITE_LOAD_CERTIFICATES app setting to the certificate thumbprint (or * for all certificates).' },
      { id: 'b', text: 'Set the CERTIFICATE_THUMBPRINT environment variable in App Service Configuration.' },
      { id: 'c', text: 'The certificate is automatically available in the local machine certificate store without any additional configuration.' },
      { id: 'd', text: 'Use Azure Key Vault references in the app settings to reference the certificate.' },
    ],
    correctOptionId: 'a',
    explanation: 'To make certificates accessible to application code running in App Service, you set the WEBSITE_LOAD_CERTIFICATES app setting to the certificate thumbprint (comma-separated for multiple, or * for all). This loads the certificate into the Windows certificate store (or makes it accessible on Linux), allowing the application to retrieve it programmatically. Without this setting, the certificate is not loaded into the app\'s accessible certificate store.',
  },

  {
    type: 'drag-drop',
    id: 'cp-198',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'Match each certificate scenario to the correct certificate type or approach in App Service.',
    items: [
      { id: 'item1', text: 'Free, automatically renewed certificate for a single custom domain' },
      { id: 'item2', text: 'Certificate covering *.contoso.com purchased from a third-party CA' },
      { id: 'item3', text: 'Certificate for a custom domain purchased and managed entirely within Azure' },
      { id: 'item4', text: 'Certificate used only by application code for client authentication, not for TLS binding' },
    ],
    targets: [
      { id: 'target1', label: 'App Service Managed Certificate (free)', correctItemId: 'item1' },
      { id: 'target2', label: 'Third-party wildcard certificate uploaded as PFX', correctItemId: 'item2' },
      { id: 'target3', label: 'App Service Certificate (paid, stored in Key Vault)', correctItemId: 'item3' },
      { id: 'target4', label: 'Public certificate loaded via WEBSITE_LOAD_CERTIFICATES', correctItemId: 'item4' },
    ],
    explanation: 'App Service Managed Certificates are free, auto-renewed, and cover single custom subdomains. Third-party wildcard certificates must be uploaded as PFX files. App Service Certificates are Azure-managed paid certificates stored in Key Vault with automatic renewal. Public certificates (without private keys) can be loaded for use in application code without TLS binding.',
  },

  {
    type: 'single-choice',
    id: 'cp-199',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'You need to configure mutual TLS (mTLS) authentication so that client certificates are required for all requests to your App Service app. Where do you enable this?',
    options: [
      { id: 'a', text: 'App Service > Authentication > Require client certificates' },
      { id: 'b', text: 'App Service > TLS/SSL settings > Client certificate mode = Require' },
      { id: 'c', text: 'App Service > Configuration > General settings > Client certificate mode = Require' },
      { id: 'd', text: 'App Service > Networking > Access restrictions > Require client certificate' },
    ],
    correctOptionId: 'c',
    explanation: 'Client certificate authentication (mTLS) is configured in App Service > Configuration > General settings under "Client certificate mode." Options include Off, Allow (optional), Require, and Optional Interactive User. Setting it to "Require" means App Service will request and require a valid client certificate for all inbound requests before they reach your application.',
  },

  {
    type: 'single-choice',
    id: 'cp-200',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure certificates and TLS for an App Service',
    stem: 'Your App Service app uses a certificate that will expire in 30 days. The certificate is an App Service Managed Certificate. What action should you take?',
    options: [
      { id: 'a', text: 'Manually renew the certificate by re-running the certificate creation wizard.' },
      { id: 'b', text: 'Purchase a new App Service Certificate from Azure Marketplace.' },
      { id: 'c', text: 'No action is required; App Service Managed Certificates are automatically renewed before expiry.' },
      { id: 'd', text: 'Export the current certificate, renew it with the CA, and re-import it.' },
    ],
    correctOptionId: 'c',
    explanation: 'App Service Managed Certificates are automatically renewed by Azure before they expire — no manual intervention is required. Azure handles the renewal process transparently. This automatic renewal is one of the key benefits of using Managed Certificates. App Service Certificates (purchased paid certificates) are also auto-renewed via Azure Key Vault.',
  },

  // ──────────────────────────────────────────────────────────────────────────
  // MAP AN EXISTING CUSTOM DNS NAME TO AN APP SERVICE (cp-201 to cp-210)
  // ──────────────────────────────────────────────────────────────────────────

  {
    type: 'single-choice',
    id: 'cp-201',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'You want to map the apex domain "contoso.com" (root domain, no subdomain) to your App Service app. Which DNS record type should you create at your DNS registrar?',
    options: [
      { id: 'a', text: 'CNAME record pointing contoso.com to contosoapp.azurewebsites.net' },
      { id: 'b', text: 'A record pointing contoso.com to the App Service inbound IP address, plus a TXT record for domain verification' },
      { id: 'c', text: 'ALIAS record pointing contoso.com to contosoapp.azurewebsites.net' },
      { id: 'd', text: 'NS record pointing contoso.com to Azure DNS nameservers' },
    ],
    correctOptionId: 'b',
    explanation: 'For apex/root domains (e.g., contoso.com without a subdomain), you cannot use a CNAME record because the DNS specification prohibits CNAME at the zone apex. You must use an A record pointing to the App Service\'s inbound IP address, plus a TXT record with the app verification ID to prove domain ownership. Some DNS providers support ALIAS or ANAME records (which behave like CNAME but are allowed at apex), but the standard Azure approach is the A + TXT combination.',
  },

  {
    type: 'single-choice',
    id: 'cp-202',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'You are mapping the subdomain "www.contoso.com" to an App Service app. Which two DNS records must you create? (This question has one correct combined answer.)',
    options: [
      { id: 'a', text: 'A CNAME record: www.contoso.com → contosoapp.azurewebsites.net; and an A record for the IP address' },
      { id: 'b', text: 'A CNAME record: www.contoso.com → contosoapp.azurewebsites.net; and a TXT record: asuid.www.contoso.com → the Custom Domain Verification ID' },
      { id: 'c', text: 'A TXT record only: www.contoso.com → the Custom Domain Verification ID' },
      { id: 'd', text: 'A CNAME record: www.contoso.com → contosoapp.azurewebsites.net; and an MX record for mail delivery' },
    ],
    correctOptionId: 'b',
    explanation: 'For a subdomain, you create a CNAME record pointing www.contoso.com to contosoapp.azurewebsites.net, and a TXT record at asuid.www.contoso.com containing the Custom Domain Verification ID from the App Service Custom domains blade. The TXT record proves you own the domain. The combination of CNAME + TXT is the standard approach for subdomain mapping.',
  },

  {
    type: 'yes-no',
    id: 'cp-203',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'Your team is mapping custom DNS names to App Service apps. Evaluate each statement.',
    statements: [
      { id: 's1', text: 'You can map the same custom domain to multiple App Service apps simultaneously.', correct: false },
      { id: 's2', text: 'The Free App Service plan tier supports custom domain mapping.', correct: false },
      { id: 's3', text: 'After adding a custom domain to an App Service app, you can validate it has been correctly configured by using the "Validate" function in the Custom domains blade.', correct: true },
    ],
    explanation: 'A custom domain can only be mapped to one App Service app at a time — you will receive an error if you try to add a domain already in use by another app. Free tier does NOT support custom domains; Shared (D1) is the minimum tier for custom domain support. The Azure portal Custom domains blade has a Validate button that checks DNS record propagation and domain ownership verification.',
  },

  {
    type: 'drag-drop',
    id: 'cp-204',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'You are configuring DNS records to map custom domains to an App Service. Match each domain mapping scenario to the required DNS record configuration.',
    items: [
      { id: 'item1', text: 'CNAME: www → contosoapp.azurewebsites.net + TXT: asuid.www → Verification ID' },
      { id: 'item2', text: 'A: @ → App Service IP + TXT: asuid → Verification ID' },
      { id: 'item3', text: 'CNAME: blog → contosoapp.azurewebsites.net + TXT: asuid.blog → Verification ID' },
      { id: 'item4', text: 'A: @ → App Service IP + CNAME: www → contosoapp.azurewebsites.net + TXT records for both' },
    ],
    targets: [
      { id: 'target1', label: 'Map www.contoso.com (subdomain)', correctItemId: 'item1' },
      { id: 'target2', label: 'Map contoso.com (apex domain only)', correctItemId: 'item2' },
      { id: 'target3', label: 'Map blog.contoso.com (subdomain)', correctItemId: 'item3' },
      { id: 'target4', label: 'Map both contoso.com and www.contoso.com', correctItemId: 'item4' },
    ],
    explanation: 'Subdomains use CNAME + TXT (at asuid.{subdomain}). Apex domains use A record + TXT (at asuid). When mapping both apex and www, you need both A and CNAME records plus corresponding TXT records for each. The asuid TXT record prefix is used for domain ownership verification in App Service.',
  },

  {
    type: 'single-choice',
    id: 'cp-205',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'You have mapped "www.contoso.com" to your App Service. The App Service plan is Standard S1. You later scale down to the Basic B1 plan. What happens to the custom domain mapping?',
    options: [
      { id: 'a', text: 'The custom domain mapping is removed automatically when scaling down to Basic.' },
      { id: 'b', text: 'The custom domain continues to work on Basic tier since Basic supports custom domains.' },
      { id: 'c', text: 'The custom domain mapping is disabled but preserved; it reactivates when you scale back up.' },
      { id: 'd', text: 'Scaling down is not possible when custom domains are configured.' },
    ],
    correctOptionId: 'b',
    explanation: 'The Basic tier supports custom domains. Scaling down from Standard to Basic does not remove custom domain mappings — the domain continues to resolve correctly. Custom domain support requires at minimum the Shared (D1) tier. The configuration is preserved across tier changes.',
  },

  {
    type: 'single-choice',
    id: 'cp-206',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'You are using Azure DNS to manage the contoso.com zone. You want to map contoso.com to your App Service without using an A record (to avoid IP address dependency). Which Azure DNS record type can be used at the apex?',
    options: [
      { id: 'a', text: 'CNAME record at the zone apex' },
      { id: 'b', text: 'ALIAS record in Azure DNS pointing to the App Service default hostname' },
      { id: 'c', text: 'SRV record at the zone apex' },
      { id: 'd', text: 'PTR record pointing to the App Service' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure DNS supports ALIAS records (also called ALIAS or Apex CNAME) that can be placed at the zone apex. An ALIAS record in Azure DNS can point to an Azure resource (including an App Service default hostname) and dynamically resolves to the correct IP addresses. This avoids the need to hardcode an IP address. This is specific to Azure DNS — not all DNS providers support apex ALIAS records.',
  },

  {
    type: 'dropdown',
    id: 'cp-207',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'Complete the following statement about custom domain verification in App Service.',
    segments: [
      { type: 'text', text: 'When adding a custom domain to App Service, the Custom Domain Verification ID is found in the ' },
      { type: 'dropdown', id: 'dd1', options: ['Custom domains blade of the App Service', 'App Service plan settings', 'Azure DNS zone configuration'], correctOption: 'Custom domains blade of the App Service' },
      { type: 'text', text: '. This ID must be placed in a TXT record with the hostname prefix ' },
      { type: 'dropdown', id: 'dd2', options: ['verify', 'asuid', 'azure-verify', 'domain-verify'], correctOption: 'asuid' },
      { type: 'text', text: '.' },
    ],
    explanation: 'The Custom Domain Verification ID is displayed in the App Service Custom domains blade. When creating the DNS TXT record for domain verification, the hostname must be prefixed with "asuid." (e.g., asuid.www for a www subdomain, or asuid for the apex domain). This "asuid" prefix is specific to Azure App Service domain verification.',
  },

  {
    type: 'single-choice',
    id: 'cp-208',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'You have a Traffic Manager profile in front of two App Service apps in different regions. You want www.contoso.com to resolve to the Traffic Manager profile, which then routes to the App Services. Which DNS record should you create in your DNS zone?',
    options: [
      { id: 'a', text: 'A record: www.contoso.com → Traffic Manager IP address' },
      { id: 'b', text: 'CNAME record: www.contoso.com → the Traffic Manager profile DNS name (e.g., contoso.trafficmanager.net)' },
      { id: 'c', text: 'CNAME record: www.contoso.com → contosoapp1.azurewebsites.net (primary region only)' },
      { id: 'd', text: 'NS record: www.contoso.com → trafficmanager.net nameservers' },
    ],
    correctOptionId: 'b',
    explanation: 'When using Traffic Manager for routing to App Services, you create a CNAME record pointing your custom domain (e.g., www.contoso.com) to the Traffic Manager profile DNS name (e.g., contoso.trafficmanager.net). Traffic Manager does not have a static IP address, so an A record is not appropriate. The Traffic Manager then routes requests to the appropriate App Service endpoint.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-209',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'You are troubleshooting a custom domain mapping for an App Service. Which two actions should you take to verify the DNS configuration is correct? (Choose two.)',
    options: [
      { id: 'a', text: 'Use the "nslookup www.contoso.com" command to verify the CNAME resolves to azurewebsites.net.' },
      { id: 'b', text: 'Click "Validate" in the App Service Custom domains blade to check DNS propagation.' },
      { id: 'c', text: 'Check the App Service plan pricing tier to confirm custom domain support.' },
      { id: 'd', text: 'Review the App Service deployment logs for domain mapping errors.' },
      { id: 'e', text: 'Restart the App Service to force DNS re-evaluation.' },
    ],
    correctOptionIds: ['a', 'b'],
    requiredSelections: 2,
    explanation: 'Using nslookup (or dig) lets you verify that the CNAME record resolves correctly to the azurewebsites.net domain. The Validate button in the Custom domains blade performs an automated check of both the CNAME/A record and the TXT verification record, showing specific errors if propagation has not completed. Deployment logs do not contain DNS mapping information, and restarting the app has no effect on DNS resolution.',
  },

  {
    type: 'single-choice',
    id: 'cp-210',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Map an existing custom DNS name to an App Service',
    stem: 'You have an App Service app with the domain app.contoso.com. You want to also configure the domain to work without a binding failure if the IP address of the app changes (e.g., after scaling or disaster recovery). Which approach provides the most resilient DNS configuration?',
    options: [
      { id: 'a', text: 'Use an A record pointing to the current outbound IP address of the App Service.' },
      { id: 'b', text: 'Use a CNAME record pointing to the default azurewebsites.net hostname of the App Service.' },
      { id: 'c', text: 'Use a static reserved IP address and create an A record to that IP.' },
      { id: 'd', text: 'Use App Service plan IP SSL binding to reserve a dedicated inbound IP.' },
    ],
    correctOptionId: 'b',
    explanation: 'Using a CNAME pointing to the azurewebsites.net hostname is the most resilient approach because the CNAME follows the App Service hostname, which always resolves to the current IP addresses managed by Azure. If the inbound IP changes (due to scaling, plan changes, or migration), the CNAME continues to work without DNS record updates. A records tied to specific IPs require manual updates if the IP changes.',
  },

  // ──────────────────────────────────────────────────────────────────────────
  // CONFIGURE BACKUP FOR AN APP SERVICE (cp-211 to cp-220)
  // ──────────────────────────────────────────────────────────────────────────

  {
    type: 'single-choice',
    id: 'cp-211',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'You need to configure automated backup for an App Service web app. Which is the minimum App Service plan tier that supports the Backup feature?',
    options: [
      { id: 'a', text: 'Free (F1)' },
      { id: 'b', text: 'Basic (B1)' },
      { id: 'c', text: 'Standard (S1)' },
      { id: 'd', text: 'Premium (P1v3)' },
    ],
    correctOptionId: 'c',
    explanation: 'The App Service Backup feature (Backup and Restore) is available starting from the Standard tier. Free and Basic tiers do not support this feature. Standard tier allows backups up to 10 GB. Premium tier supports backups up to 10 GB as well. Isolated tier also supports backup.',
  },

  {
    type: 'single-choice',
    id: 'cp-212',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'You are configuring scheduled backup for an App Service. The backup destination must be an Azure Storage account. What specific storage resource is used as the backup destination?',
    options: [
      { id: 'a', text: 'An Azure Blob Storage container with a Shared Access Signature (SAS) URL' },
      { id: 'b', text: 'An Azure File Share mounted to the App Service' },
      { id: 'c', text: 'Any Azure Storage account without additional configuration' },
      { id: 'd', text: 'An Azure Blob container with a Storage account key' },
    ],
    correctOptionId: 'a',
    explanation: 'App Service backup requires an Azure Blob Storage container and a Shared Access Signature (SAS) URL that grants write permissions. The SAS URL is used to authorize the backup operation without storing the storage account key. File shares are not supported as backup destinations. A raw storage account key is not the required format.',
  },

  {
    type: 'yes-no',
    id: 'cp-213',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    scenario: 'You are planning the backup strategy for an App Service application with a connected Azure SQL Database. Evaluate each statement.',
    statements: [
      { id: 's1', text: 'App Service backup can include the connected Azure SQL Database content in the same backup file.', correct: true },
      { id: 's2', text: 'App Service backup captures both the application files and the app configuration settings by default.', correct: true },
      { id: 's3', text: 'You can restore an App Service backup to a different App Service app in a different region.', correct: true },
    ],
    explanation: 'App Service backup can include linked databases (Azure SQL Database, Azure Database for MySQL, Azure Database for PostgreSQL) in the same backup. The backup captures both app content (files) and configuration. A backup can be restored to a different app and even a different region, making it useful for disaster recovery scenarios.',
  },

  {
    type: 'single-choice',
    id: 'cp-214',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'You configure a daily scheduled backup for an App Service at 2:00 AM. The backup retention is set to 30 days. How many backup files will be retained after 45 days of operation?',
    options: [
      { id: 'a', text: '45 backups (all backups since creation)' },
      { id: 'b', text: '30 backups (the most recent 30 days)' },
      { id: 'c', text: '15 backups (only backups from the last 15 days are kept after 45 days)' },
      { id: 'd', text: '1 backup (only the most recent backup is retained)' },
    ],
    correctOptionId: 'b',
    explanation: 'The backup retention setting specifies how many days of backups to keep. With a 30-day retention and daily backups, after 45 days there will be approximately 30 backup files — the oldest backups beyond the 30-day window are automatically deleted. The system retains the most recent 30 daily backups.',
  },

  {
    type: 'dropdown',
    id: 'cp-215',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'Complete the statement about App Service backup limitations.',
    segments: [
      { type: 'text', text: 'The maximum backup size for an App Service on the Standard or Premium tier is ' },
      { type: 'dropdown', id: 'dd1', options: ['1 GB', '10 GB', '50 GB', '100 GB'], correctOption: '10 GB' },
      { type: 'text', text: '. Backups larger than this limit require using ' },
      { type: 'dropdown', id: 'dd2', options: ['Azure Site Recovery', 'custom backup scripts with the Kudu REST API', 'Azure Data Factory'], correctOption: 'custom backup scripts with the Kudu REST API' },
      { type: 'text', text: '.' },
    ],
    explanation: 'App Service built-in backup supports backups up to 10 GB (including linked databases). For apps with more content than 10 GB, the built-in backup feature will fail. In those cases, you must implement custom backup solutions — a common approach is to use the Kudu REST API to download the app content directly to storage.',
  },

  {
    type: 'single-choice',
    id: 'cp-216',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'You need to perform a partial restore of an App Service backup — specifically, you only want to restore the database content without overwriting the application files. What is the correct approach?',
    options: [
      { id: 'a', text: 'Use the App Service Restore wizard and select "Restore database only" option.' },
      { id: 'b', text: 'Download the backup ZIP file from Storage, extract the database backup, and restore it directly to the database service.' },
      { id: 'c', text: 'Create a new App Service from the backup and manually copy only the database files.' },
      { id: 'd', text: 'App Service restore always restores all components; partial restore is not supported.' },
    ],
    correctOptionId: 'b',
    explanation: 'App Service backup creates a ZIP archive containing the app content and separate SQL/MySQL backup files. The built-in Restore feature in the portal restores everything together. For partial restores (database only), you must manually download the backup ZIP from Azure Storage, extract the database backup file (e.g., .bacpac for SQL), and import it directly into the database service.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-217',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'Which two types of databases can be included in an App Service built-in backup? (Choose two.)',
    options: [
      { id: 'a', text: 'Azure SQL Database' },
      { id: 'b', text: 'Azure Cosmos DB (NoSQL)' },
      { id: 'c', text: 'Azure Database for MySQL' },
      { id: 'd', text: 'Azure Cache for Redis' },
      { id: 'e', text: 'Azure Table Storage' },
    ],
    correctOptionIds: ['a', 'c'],
    requiredSelections: 2,
    explanation: 'App Service built-in backup supports Azure SQL Database, Azure Database for MySQL, and Azure Database for PostgreSQL as linked database backends. Cosmos DB, Redis Cache, and Azure Table Storage are not supported for automatic inclusion in App Service backups and require separate backup strategies.',
  },

  {
    type: 'single-choice',
    id: 'cp-218',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'You want to configure backup for an App Service using Azure CLI. Which command enables a scheduled backup that runs daily and stores backups in a storage container with a SAS URL?',
    options: [
      { id: 'a', text: 'az webapp config backup create --resource-group MyRG --webapp-name MyApp --container-url "SAS_URL" --frequency 1d --retain-one true' },
      { id: 'b', text: 'az backup protection enable-for-webapp --resource-group MyRG --vault-name MyVault --webapp MyApp --policy MyPolicy' },
      { id: 'c', text: 'az webapp config backup update --resource-group MyRG --webapp-name MyApp --container-url "SAS_URL" --frequency 1440 --retention-period-in-days 30' },
      { id: 'd', text: 'az storage blob upload --account-name MyStorage --container backup --source /home/site/wwwroot' },
    ],
    correctOptionId: 'a',
    explanation: 'The "az webapp config backup create" command configures a new scheduled backup for an App Service. Key parameters include --container-url (SAS URL of the storage container), --frequency (e.g., 1d for daily), and --retain-one. Option C uses "update" which modifies existing backup config. Option B is for Azure Backup vault (different service). Option D is a raw storage upload, not App Service backup.',
  },

  {
    type: 'drag-drop',
    id: 'cp-219',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'Match each App Service backup configuration requirement to the correct Azure resource or setting.',
    items: [
      { id: 'item1', text: 'Azure Blob Storage container SAS URL' },
      { id: 'item2', text: 'Standard or higher App Service plan' },
      { id: 'item3', text: 'Backup frequency and retention period settings' },
      { id: 'item4', text: 'Connection strings for linked databases' },
    ],
    targets: [
      { id: 'target1', label: 'Where backup files are stored', correctItemId: 'item1' },
      { id: 'target2', label: 'Plan tier prerequisite for backup', correctItemId: 'item2' },
      { id: 'target3', label: 'Scheduled backup automation configuration', correctItemId: 'item3' },
      { id: 'target4', label: 'Required to include database in backup', correctItemId: 'item4' },
    ],
    explanation: 'App Service backup requires: a Blob Storage container SAS URL as the destination; Standard tier or higher as a prerequisite; frequency and retention settings for scheduled backups; and connection strings must be configured in the App Service for the backup to include linked database content.',
  },

  {
    type: 'single-choice',
    id: 'cp-220',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure backup for an App Service',
    stem: 'An App Service backup fails with the error "The backup storage account container does not exist or the SAS URL has expired." What are the two most likely causes and the correct remediation?',
    options: [
      { id: 'a', text: 'The App Service plan is on Basic tier; upgrade to Standard to enable backup storage access.' },
      { id: 'b', text: 'The SAS URL has expired or the storage container was deleted; regenerate the SAS URL with a new expiry date and update the backup configuration.' },
      { id: 'c', text: 'The App Service does not have a managed identity; enable managed identity and assign Storage Blob Data Contributor role.' },
      { id: 'd', text: 'The storage account firewall is blocking App Service; add the App Service outbound IPs to the storage account firewall.' },
    ],
    correctOptionId: 'b',
    explanation: 'App Service backup uses a SAS URL for authorization. If the SAS URL has expired (SAS tokens have an expiry time) or the target storage container was deleted, the backup will fail with this error. The remediation is to generate a new SAS URL with a fresh expiry date and update the App Service backup configuration with the new URL. App Service backup does not use managed identity for storage access.',
  },

  // ──────────────────────────────────────────────────────────────────────────
  // CONFIGURE NETWORKING SETTINGS FOR AN APP SERVICE (cp-221 to cp-230)
  // ──────────────────────────────────────────────────────────────────────────

  {
    type: 'single-choice',
    id: 'cp-221',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'You need your App Service web app to access resources in an Azure Virtual Network (VNet), such as a VM running an internal API. Which feature enables outbound connectivity from App Service to VNet resources?',
    options: [
      { id: 'a', text: 'Private Endpoint' },
      { id: 'b', text: 'VNet Integration' },
      { id: 'c', text: 'Access Restrictions' },
      { id: 'd', text: 'Hybrid Connections' },
    ],
    correctOptionId: 'b',
    explanation: 'VNet Integration enables outbound traffic from App Service to flow into an Azure Virtual Network. This allows the app to access VNet resources (VMs, databases, other services). Private Endpoint provides inbound private connectivity (secures the App Service inbound endpoint). Access Restrictions control inbound traffic rules. Hybrid Connections is an alternative for on-premises connectivity.',
  },

  {
    type: 'single-choice',
    id: 'cp-222',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'You want to restrict inbound access to your App Service so that only traffic from your corporate network (IP range 203.0.113.0/24) is allowed. Which feature should you configure?',
    options: [
      { id: 'a', text: 'VNet Integration with a subnet filter' },
      { id: 'b', text: 'App Service Access Restrictions (IP-based rules)' },
      { id: 'c', text: 'Azure Application Gateway WAF policy' },
      { id: 'd', text: 'Network Security Group on the App Service subnet' },
    ],
    correctOptionId: 'b',
    explanation: 'App Service Access Restrictions allow you to define allow/deny rules based on IP addresses, IP ranges (CIDR), Virtual Network subnets, or service tags. Configuring an allow rule for 203.0.113.0/24 and denying all other traffic restricts access to the specified corporate IP range. VNet Integration handles outbound, not inbound filtering. NSGs cannot be applied directly to App Service (only to ASE or VNet subnets).',
  },

  {
    type: 'yes-no',
    id: 'cp-223',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    scenario: 'Your team is designing the network architecture for an App Service deployment. Evaluate each statement.',
    statements: [
      { id: 's1', text: 'Regional VNet Integration allows the App Service to reach resources in peered VNets and on-premises networks connected via VPN or ExpressRoute.', correct: true },
      { id: 's2', text: 'A Private Endpoint for an App Service removes the app\'s public endpoint and makes it accessible only via the private IP within the VNet.', correct: false },
      { id: 's3', text: 'Hybrid Connections can be used to connect an App Service to on-premises resources without requiring VNet Integration or VPN.', correct: true },
    ],
    explanation: 'Regional VNet Integration routes outbound traffic through a VNet, which can then reach peered VNets and on-premises networks via VPN/ExpressRoute. A Private Endpoint provides a private IP for the App Service within a VNet but does NOT remove the public endpoint by default — you must also configure access restrictions to deny public traffic. Hybrid Connections uses an Azure Service Bus relay to connect App Service to on-premises endpoints without requiring a VPN or VNet.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-224',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'You are configuring VNet Integration for an App Service. Which two requirements must be met for Regional VNet Integration? (Choose two.)',
    options: [
      { id: 'a', text: 'The App Service plan must be Standard, Premium, or Isolated tier.' },
      { id: 'b', text: 'The VNet must be in the same region as the App Service.' },
      { id: 'c', text: 'A dedicated subnet must be delegated to Microsoft.Web/serverFarms.' },
      { id: 'd', text: 'The App Service must use a Linux OS App Service plan.' },
      { id: 'e', text: 'The VNet must have at least a /16 address space.' },
    ],
    correctOptionIds: ['b', 'c'],
    requiredSelections: 2,
    explanation: 'Regional VNet Integration requires: (1) the VNet must be in the same Azure region as the App Service, and (2) a dedicated subnet delegated to Microsoft.Web/serverFarms. The App Service plan must be Basic tier or higher (not Free/Shared), and the subnet must be empty and delegated. Both Windows and Linux are supported. The VNet address space size does not need to be /16 — any appropriately sized subnet works.',
  },

  {
    type: 'single-choice',
    id: 'cp-225',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'Your App Service has VNet Integration configured. The app needs to resolve private DNS names for resources in the VNet (e.g., an internal SQL Server using a private DNS zone). What setting must you configure on the App Service?',
    options: [
      { id: 'a', text: 'Set WEBSITE_DNS_SERVER app setting to the VNet\'s DNS server IP.' },
      { id: 'b', text: 'Set WEBSITE_VNET_ROUTE_ALL to 1 and configure a private DNS zone linked to the VNet.' },
      { id: 'c', text: 'Enable the "Route All" setting in VNet Integration and ensure the private DNS zone is linked to the VNet.' },
      { id: 'd', text: 'Both B and C describe the same correct configuration.' },
    ],
    correctOptionId: 'd',
    explanation: 'To resolve private DNS names via VNet Integration, you need two things: (1) enable Route All traffic (WEBSITE_VNET_ROUTE_ALL=1 or the Route All toggle in the portal) so DNS queries go through the VNet, and (2) ensure the private DNS zone is linked to the integrated VNet so the VNet DNS can resolve private names. WEBSITE_DNS_SERVER can also be set if using a custom DNS server, but the private DNS zone link approach is the recommended Azure-native method.',
  },

  {
    type: 'dropdown',
    id: 'cp-226',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'Complete the statement about App Service Private Endpoints.',
    segments: [
      { type: 'text', text: 'A Private Endpoint for an App Service assigns a ' },
      { type: 'dropdown', id: 'dd1', options: ['public IP address', 'private IP address from the VNet subnet', 'shared IP from Azure Front Door'], correctOption: 'private IP address from the VNet subnet' },
      { type: 'text', text: ' to the App Service, enabling access from within the VNet. To fully block public internet access, you must also configure ' },
      { type: 'dropdown', id: 'dd2', options: ['VNet Integration', 'Access Restrictions to deny all public traffic', 'Azure Firewall DNAT rules'], correctOption: 'Access Restrictions to deny all public traffic' },
      { type: 'text', text: '.' },
    ],
    explanation: 'A Private Endpoint assigns a private IP from a VNet subnet to the App Service\'s inbound endpoint. However, the App Service still has its public endpoint accessible by default. To make the app fully private, you must also configure Access Restrictions to deny all inbound traffic from the internet (the private endpoint traffic bypasses access restrictions because it comes from the VNet).',
  },

  {
    type: 'drag-drop',
    id: 'cp-227',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'Match each networking scenario to the correct App Service networking feature.',
    items: [
      { id: 'item1', text: 'VNet Integration' },
      { id: 'item2', text: 'Private Endpoint' },
      { id: 'item3', text: 'Access Restrictions' },
      { id: 'item4', text: 'Hybrid Connections' },
    ],
    targets: [
      { id: 'target1', label: 'App Service needs to call an internal microservice in the same VNet', correctItemId: 'item1' },
      { id: 'target2', label: 'Restrict App Service inbound endpoint to be accessible only from a VNet private IP', correctItemId: 'item2' },
      { id: 'target3', label: 'Allow only specific public IP ranges to access the App Service', correctItemId: 'item3' },
      { id: 'target4', label: 'App Service needs to connect to an on-premises SQL Server without VPN', correctItemId: 'item4' },
    ],
    explanation: 'VNet Integration routes outbound traffic from the App Service into a VNet (for calling internal services). Private Endpoint provides an inbound private IP in a VNet. Access Restrictions filter inbound traffic by IP, service tag, or VNet subnet. Hybrid Connections uses Service Bus relay for outbound connectivity to on-premises resources without requiring VPN.',
  },

  {
    type: 'single-choice',
    id: 'cp-228',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'You have placed an Application Gateway with WAF in front of your App Service. You want to ensure that only the Application Gateway can send requests to the App Service, blocking direct internet access. Which combination of settings achieves this?',
    options: [
      { id: 'a', text: 'Configure Access Restrictions to allow the Application Gateway\'s subnet and deny all other traffic.' },
      { id: 'b', text: 'Enable Private Endpoint on the App Service and place it in the same VNet as the Application Gateway.' },
      { id: 'c', text: 'Both A and B are valid approaches; A uses service tag "AppGateway" in Access Restrictions, B uses private connectivity.' },
      { id: 'd', text: 'Configure the App Service to only respond to requests with the X-Forwarded-For header matching the Application Gateway IP.' },
    ],
    correctOptionId: 'c',
    explanation: 'Both approaches work: (A) Using Access Restrictions with the Application Gateway\'s VNet subnet (or the AzureFrontDoor.Backend / specific subnet service tag) blocks direct internet access while allowing App Gateway traffic. (B) Placing a Private Endpoint in the same VNet as the Application Gateway means only VNet traffic can reach the App Service. Using X-Forwarded-For headers alone is not secure as headers can be spoofed.',
  },

  {
    type: 'single-choice',
    id: 'cp-229',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'You need to configure App Service Access Restrictions to block all traffic EXCEPT from Azure Front Door. Which service tag should you use in the allow rule?',
    options: [
      { id: 'a', text: 'AzureFrontDoor.Backend' },
      { id: 'b', text: 'AzureFrontDoor.Frontend' },
      { id: 'c', text: 'AzureCDN' },
      { id: 'd', text: 'Internet' },
    ],
    correctOptionId: 'a',
    explanation: 'The service tag "AzureFrontDoor.Backend" represents the IP ranges used by Azure Front Door when sending traffic to origin backends (your App Service). Using this service tag in an Access Restriction allow rule, combined with a deny-all rule at lower priority, ensures only Azure Front Door can reach your App Service. "AzureFrontDoor.Frontend" represents the IPs used by clients connecting to Front Door.',
  },

  {
    type: 'single-choice',
    id: 'cp-230',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure networking settings for an App Service',
    stem: 'Your App Service uses Regional VNet Integration. You want all outbound traffic (including calls to Azure Storage and other public endpoints) to go through the VNet and your Azure Firewall, not directly to the internet. Which configuration is required?',
    options: [
      { id: 'a', text: 'Set WEBSITE_VNET_ROUTE_ALL=1 in App Service application settings and configure a User Defined Route (UDR) on the integration subnet to route 0.0.0.0/0 to the Azure Firewall.' },
      { id: 'b', text: 'Enable VNet Integration and configure a Network Security Group on the integration subnet to block outbound internet.' },
      { id: 'c', text: 'Enable the "Route All" toggle in VNet Integration settings; no additional routing configuration is needed.' },
      { id: 'd', text: 'Configure Private Endpoints for all Azure services the app uses (Storage, SQL, etc.).' },
    ],
    correctOptionId: 'a',
    explanation: 'To force all outbound traffic from App Service through Azure Firewall via VNet Integration, you need two things: (1) WEBSITE_VNET_ROUTE_ALL=1 (or the Route All toggle), which causes all outbound traffic (not just RFC1918) to flow through the VNet, and (2) a User Defined Route (UDR) on the integration subnet with a default route (0.0.0.0/0) pointing to the Azure Firewall\'s private IP as the next hop. NSGs alone cannot redirect traffic to a firewall.',
  },

  // ──────────────────────────────────────────────────────────────────────────
  // CONFIGURE DEPLOYMENT SLOTS FOR AN APP SERVICE (cp-231 to cp-240)
  // ──────────────────────────────────────────────────────────────────────────

  {
    type: 'single-choice',
    id: 'cp-231',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'You have an App Service app on the Standard S1 plan with a staging deployment slot. You deploy new code to the staging slot and validate it. You then perform a slot swap between staging and production. What happens to the instances during the swap?',
    options: [
      { id: 'a', text: 'Azure restarts production instances with the staging code immediately; there is downtime during the restart.' },
      { id: 'b', text: 'Azure warms up the staging instances, then redirects incoming requests to the (formerly staging) instances, and the previous production code becomes the new staging slot — no downtime.' },
      { id: 'c', text: 'The staging code is copied to production file system and production is restarted with brief downtime.' },
      { id: 'd', text: 'A new set of instances is provisioned for production while old instances are decommissioned.' },
    ],
    correctOptionId: 'b',
    explanation: 'App Service slot swap performs a warm swap: Azure first warms up instances in the target (staging) slot with the production configuration (sticky settings), then switches the router to send traffic to those warmed instances. The previously production instances become the new staging slot with the old code. This provides zero-downtime deployments and an immediate rollback option (swap back).',
  },

  {
    type: 'single-choice',
    id: 'cp-232',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'You perform a slot swap between the staging slot and the production slot. You discover a critical bug in production immediately after the swap. What is the fastest way to revert to the previous production version?',
    options: [
      { id: 'a', text: 'Redeploy from source control to the production slot.' },
      { id: 'b', text: 'Perform another slot swap between staging and production (swap back).' },
      { id: 'c', text: 'Restore from the most recent App Service backup.' },
      { id: 'd', text: 'Scale out the App Service plan to run the old version on additional instances.' },
    ],
    correctOptionId: 'b',
    explanation: 'After a slot swap, the old production code is now in the staging slot. Simply swapping back (performing another swap between staging and production) immediately reverts production to the previous code. This is one of the primary benefits of deployment slots — fast rollback without redeployment or restore operations.',
  },

  {
    type: 'dropdown',
    id: 'cp-233',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'Complete the statement about slot-specific settings (sticky settings) in App Service deployment slots.',
    segments: [
      { type: 'text', text: 'Settings marked as deployment slot settings (sticky) ' },
      { type: 'dropdown', id: 'dd1', options: ['are swapped along with the slot content', 'stay with the slot and are NOT swapped', 'are deleted during a swap'], correctOption: 'stay with the slot and are NOT swapped' },
      { type: 'text', text: '. This means the production slot always retains its production ' },
      { type: 'dropdown', id: 'dd2', options: ['application code', 'connection strings and app settings marked as sticky', 'deployment source configuration'], correctOption: 'connection strings and app settings marked as sticky' },
      { type: 'text', text: ' after a swap.' },
    ],
    explanation: 'Sticky settings (slot settings) are bound to the slot, not the code. They do NOT travel with the code when a swap occurs. This is how you ensure the production slot always connects to the production database and the staging slot connects to the staging database, regardless of which code version is deployed. Non-sticky settings travel with the code during a swap.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-234',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'You are configuring deployment slots for an App Service. Which two statements correctly describe deployment slot behavior? (Choose two.)',
    options: [
      { id: 'a', text: 'Each deployment slot has its own unique hostname (e.g., myapp-staging.azurewebsites.net).' },
      { id: 'b', text: 'Deployment slots share the same App Service plan resources (CPU, memory) as the production slot.' },
      { id: 'c', text: 'You can configure auto-swap so that a push to a slot automatically triggers a swap to production.' },
      { id: 'd', text: 'Deployment slots require a separate App Service plan for isolation.' },
      { id: 'e', text: 'Only the Premium V3 tier supports the auto-swap feature.' },
    ],
    correctOptionIds: ['a', 'b'],
    requiredSelections: 2,
    explanation: 'Each deployment slot gets its own unique hostname ({appname}-{slotname}.azurewebsites.net) and is accessible independently. All slots in an App Service plan share the underlying compute resources of that plan. Slots do NOT require separate plans — they run on the same plan. Auto-swap is available on Standard tier and above, not only Premium V3.',
  },

  {
    type: 'single-choice',
    id: 'cp-235',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'You want to gradually shift production traffic to a new version deployed in the staging slot — for example, sending 20% of traffic to staging and 80% to production. Which App Service feature enables this?',
    options: [
      { id: 'a', text: 'Azure Traffic Manager weighted routing between two App Service apps' },
      { id: 'b', text: 'App Service Traffic Splitting (testing in production) using the deployment slot\'s traffic percentage setting' },
      { id: 'c', text: 'Azure Application Gateway with weighted backend pools' },
      { id: 'd', text: 'Azure Front Door origin group with weighted routing' },
    ],
    correctOptionId: 'b',
    explanation: 'App Service supports Traffic Splitting (also called Testing in Production) where you can specify what percentage of traffic should be routed to a specific deployment slot. Setting the staging slot to 20% means 20% of production requests are automatically sent to the staging slot, allowing gradual canary deployments or A/B testing without external infrastructure.',
  },

  {
    type: 'yes-no',
    id: 'cp-236',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    scenario: 'Your team is implementing a deployment slot strategy for a high-traffic production App Service. Evaluate each statement.',
    statements: [
      { id: 's1', text: 'When you configure Auto-Swap for the staging slot, App Service will automatically swap staging to production after a successful deployment to staging.', correct: true },
      { id: 's2', text: 'Custom domain names can be configured on individual deployment slots (not just the production slot).', correct: true },
      { id: 's3', text: 'During a slot swap, App Service scales up the target slot to match the source slot instance count before swapping.', correct: false },
    ],
    explanation: 'Auto-Swap is a feature that triggers an automatic swap to a target slot (usually production) after a deployment to the source slot completes. Custom domains can be mapped to any slot, not just production. During a swap, Azure does NOT change the instance count — both slots run on the same App Service plan resources, and the swap is purely a routing change (after warm-up), not a scale operation.',
  },

  {
    type: 'drag-drop',
    id: 'cp-237',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'Match each deployment slot configuration item to whether it is swapped or stays with the slot during a slot swap.',
    items: [
      { id: 'item1', text: 'Stays with the slot (sticky / not swapped)' },
      { id: 'item2', text: 'Travels with the code (swapped)' },
    ],
    targets: [
      { id: 'target1', label: 'App setting marked as "Deployment slot setting"', correctItemId: 'item1' },
      { id: 'target2', label: 'App setting NOT marked as deployment slot setting', correctItemId: 'item2' },
      { id: 'target3', label: 'Connection string marked as "Deployment slot setting"', correctItemId: 'item1' },
      { id: 'target4', label: 'The deployed application code and content', correctItemId: 'item2' },
    ],
    explanation: 'During a slot swap, settings marked as "Deployment slot setting" (sticky) remain with the slot — production keeps production settings, staging keeps staging settings. Non-sticky app settings and connection strings travel with the swapped code. The application code, files, and content are also swapped. Understanding what gets swapped vs. stays is critical for the AZ-104 exam.',
  },

  {
    type: 'single-choice',
    id: 'cp-238',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'You want to use Azure CLI to swap the "staging" deployment slot with the production slot for a web app named "ContosoWeb" in resource group "WebRG". Which command performs this swap?',
    options: [
      { id: 'a', text: 'az webapp deployment slot swap --name ContosoWeb --resource-group WebRG --slot staging --target-slot production' },
      { id: 'b', text: 'az webapp swap --name ContosoWeb --resource-group WebRG --source staging --target production' },
      { id: 'c', text: 'az appservice slot swap --webapp ContosoWeb --resource-group WebRG --slot1 staging --slot2 production' },
      { id: 'd', text: 'az webapp deployment swap --name ContosoWeb --resource-group WebRG --from staging --to production' },
    ],
    correctOptionId: 'a',
    explanation: 'The correct command is "az webapp deployment slot swap" with --name (web app name), --resource-group, --slot (source slot, e.g., staging), and --target-slot (destination, e.g., production). When swapping with production, --target-slot can be omitted as production is the default target. The other command forms do not exist in Azure CLI.',
  },

  {
    type: 'single-choice',
    id: 'cp-239',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'You have configured a CI/CD pipeline in Azure DevOps that deploys to the staging slot and then performs a slot swap. During the swap warm-up phase, the staging instances fail the health check. What happens?',
    options: [
      { id: 'a', text: 'The swap continues and production traffic is sent to the failing instances.' },
      { id: 'b', text: 'The swap is aborted and the staging slot retains the new code while production remains on the old code.' },
      { id: 'c', text: 'Azure automatically rolls back the staging slot to the previous code before the failed deployment.' },
      { id: 'd', text: 'Azure sends an alert but completes the swap anyway, requiring manual rollback.' },
    ],
    correctOptionId: 'b',
    explanation: 'If the warm-up phase fails (health checks fail on the staging instances during pre-swap warm-up), Azure aborts the swap operation. Production continues running the existing code without disruption. The staging slot retains the new (failed) code for investigation. Azure does NOT automatically roll back the staging slot code — you must fix the issue in staging and retry. This safety mechanism is why deployment slots are valuable for zero-downtime deployments.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-240',
    sectionId: 'compute',
    subsectionId: 'app-service',
    bulletPoint: 'Configure deployment slots for an App Service',
    stem: 'You are designing a deployment strategy using App Service slots. Which two practices are Azure best practices for using deployment slots? (Choose two.)',
    options: [
      { id: 'a', text: 'Deploy new code to a staging slot, validate in staging, then swap to production.' },
      { id: 'b', text: 'Mark environment-specific settings (production database connection string) as deployment slot settings so they are not swapped.' },
      { id: 'c', text: 'Deploy new code directly to the production slot and use a backup to roll back if needed.' },
      { id: 'd', text: 'Use separate App Service plans for each slot to ensure production isolation.' },
      { id: 'e', text: 'Set the minimum instance count to 0 for staging slots to save costs.' },
    ],
    correctOptionIds: ['a', 'b'],
    requiredSelections: 2,
    explanation: 'Azure best practices for deployment slots include: (1) using staging slots to pre-warm and validate new code before swapping to production — enabling zero-downtime deployments and instant rollback; and (2) marking environment-specific settings as slot settings (sticky) to ensure each slot connects to the correct database/services regardless of swaps. Deploying directly to production bypasses the benefits of slots. Slots share the same plan (separate plans defeat the purpose). Setting staging to 0 instances means the app is not warmed up before swapping.',
  },
];
