import { Question } from '../types';

export const containerQuestions: Question[] = [
  // ─── cp-121 to cp-130: Create and manage an Azure container registry ───

  {
    type: 'single-choice',
    id: 'cp-121',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'You need to create an Azure Container Registry (ACR) that supports geo-replication to multiple Azure regions. Which SKU must you choose?',
    options: [
      { id: 'a', text: 'Basic' },
      { id: 'b', text: 'Standard' },
      { id: 'c', text: 'Premium' },
      { id: 'd', text: 'Classic' },
    ],
    correctOptionId: 'c',
    explanation: 'Geo-replication is a feature exclusive to the Premium SKU of Azure Container Registry. Basic and Standard SKUs do not support geo-replication. The Classic SKU is deprecated and should not be used for new deployments.',
  },

  {
    type: 'single-choice',
    id: 'cp-122',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'A developer needs to push a Docker image to an Azure Container Registry named "mycompanyacr". Which command correctly tags and pushes the image?',
    options: [
      { id: 'a', text: 'docker push mycompanyacr/myimage:v1' },
      { id: 'b', text: 'docker push mycompanyacr.azurecr.io/myimage:v1' },
      { id: 'c', text: 'az acr push --name mycompanyacr --image myimage:v1' },
      { id: 'd', text: 'docker push azure://mycompanyacr/myimage:v1' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Container Registry uses the fully qualified domain name format <registry-name>.azurecr.io as the login server. Before pushing, you must tag the image as mycompanyacr.azurecr.io/myimage:v1 and authenticate with "az acr login --name mycompanyacr" or "docker login mycompanyacr.azurecr.io". Option A is missing the .azurecr.io domain; option C is not a valid Azure CLI command; option D uses an incorrect scheme.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-123',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'You are configuring access to an Azure Container Registry. Which THREE authentication methods are supported for pushing and pulling images? (Select 3)',
    options: [
      { id: 'a', text: 'Azure Active Directory service principal with assigned AcrPush role' },
      { id: 'b', text: 'ACR admin user account (username/password)' },
      { id: 'c', text: 'Managed identity assigned the AcrPull role' },
      { id: 'd', text: 'Shared Access Signature (SAS) token generated from the registry' },
      { id: 'e', text: 'Azure AD user identity via "az acr login"' },
    ],
    correctOptionIds: ['a', 'b', 'c'],
    requiredSelections: 3,
    explanation: 'Azure Container Registry supports authentication via: (1) Azure AD service principals with ACR role assignments (AcrPull, AcrPush, etc.), (2) the registry admin user (a single built-in account with username and two passwords that can be enabled on the registry), and (3) managed identities assigned ACR roles — commonly used for Azure services like ACI, AKS, and App Service. Individual Azure AD user identities via "az acr login" are also valid but option E is not among the three. SAS tokens (option D) are not a supported authentication mechanism for ACR; ACR uses its own token-based system distinct from storage SAS tokens.',
  },

  {
    type: 'dropdown',
    id: 'cp-124',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'Complete the following statement about Azure Container Registry tasks.',
    segments: [
      { type: 'text', text: 'An ACR Task that automatically triggers a container image build when source code is committed to a GitHub repository is called a ' },
      {
        type: 'dropdown',
        id: 'dd1',
        options: ['Quick task', 'Source-triggered task', 'Base image update task', 'Scheduled task'],
        correctOption: 'Source-triggered task',
      },
      { type: 'text', text: '. To rebuild dependent images when a base image is updated in the same or a different registry, you would configure a ' },
      {
        type: 'dropdown',
        id: 'dd2',
        options: ['Quick task', 'Source-triggered task', 'Base image update task', 'Scheduled task'],
        correctOption: 'Base image update task',
      },
      { type: 'text', text: '.' },
    ],
    explanation: 'ACR Tasks supports four task types: (1) Quick tasks ("az acr build") for on-demand single builds without a local Docker daemon; (2) Source-triggered tasks that fire on Git commits or pull requests via webhooks; (3) Base image update tasks that detect when a base image (FROM directive) has been updated and rebuild dependent images; and (4) Scheduled tasks that run on a cron schedule. Understanding the differences is important for automating container image lifecycle management.',
  },

  {
    type: 'single-choice',
    id: 'cp-125',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'Your organization requires that container images stored in Azure Container Registry be scanned for vulnerabilities and that only signed images can be deployed. Which feature of ACR Premium enables image signature enforcement?',
    options: [
      { id: 'a', text: 'ACR Geo-replication' },
      { id: 'b', text: 'Content Trust (Docker Notary)' },
      { id: 'c', text: 'ACR Webhooks' },
      { id: 'd', text: 'Repository-scoped tokens' },
    ],
    correctOptionId: 'b',
    explanation: 'Content Trust in Azure Container Registry uses Docker Notary v1 to enable image signing and signature verification. When Content Trust is enabled on the registry and the DOCKER_CONTENT_TRUST=1 environment variable is set on the client, only images with valid cryptographic signatures can be pulled. This is a Premium SKU feature. Geo-replication handles multi-region redundancy, webhooks trigger external systems on registry events, and repository-scoped tokens provide fine-grained access control — none of these enforce image signing.',
  },

  {
    type: 'yes-no',
    id: 'cp-126',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    scenario: 'Your company uses an Azure Container Registry (Premium SKU) with the following configuration: the registry firewall is enabled with "Allow access from selected networks" and no virtual network rules or IP rules have been added. A developer wants to import a public image from Docker Hub into the registry using "az acr import". Review each statement and determine whether it is true.',
    statements: [
      {
        id: 's1',
        text: 'The "az acr import" command will fail because the registry firewall blocks the import operation when no network rules are configured.',
        correct: false,
      },
      {
        id: 's2',
        text: 'Enabling the "Allow trusted Microsoft services to access this container registry" exception would allow ACR import to succeed even with the firewall restricting public access.',
        correct: true,
      },
      {
        id: 's3',
        text: 'The ACR admin user credentials can be used to bypass the firewall restriction for import operations.',
        correct: false,
      },
    ],
    explanation: 'When the ACR firewall is set to selected networks with no rules, the registry blocks network access including import operations. However, "az acr import" is performed by the ACR service itself (a trusted Microsoft service), not through the public endpoint. Enabling the "Allow trusted Microsoft services" firewall exception permits ACR to perform operations like image import even when public access is otherwise restricted. Admin user credentials (s3) control authentication but do not bypass firewall network restrictions — authentication and network access are separate enforcement layers.',
  },

  {
    type: 'single-choice',
    id: 'cp-127',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'You need to grant a CI/CD pipeline running in Azure DevOps the minimum permissions to push images to an Azure Container Registry without giving it pull or administrative access. Which built-in ACR role should you assign to the pipeline service principal?',
    options: [
      { id: 'a', text: 'AcrPull' },
      { id: 'b', text: 'AcrPush' },
      { id: 'c', text: 'AcrDelete' },
      { id: 'd', text: 'Owner' },
    ],
    correctOptionId: 'b',
    explanation: 'AcrPush is the built-in role that grants permissions to push (upload) and pull images and artifacts. Although AcrPush includes pull permissions (needed to check what layers already exist), it is the least-privileged role that enables pushing. AcrPull grants read-only pull access. AcrDelete adds deletion capabilities. Owner grants full control over the registry resource including access management. For a CI/CD pipeline that only needs to push images, AcrPush is the correct minimal role to assign.',
  },

  {
    type: 'drag-drop',
    id: 'cp-128',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'Match each Azure Container Registry SKU to its correct storage limit per registry.',
    items: [
      { id: 'item1', text: '10 GiB included storage' },
      { id: 'item2', text: '100 GiB included storage' },
      { id: 'item3', text: '500 GiB included storage' },
    ],
    targets: [
      { id: 'target1', label: 'Basic SKU', correctItemId: 'item1' },
      { id: 'target2', label: 'Standard SKU', correctItemId: 'item2' },
      { id: 'target3', label: 'Premium SKU', correctItemId: 'item3' },
    ],
    explanation: 'Azure Container Registry storage limits by SKU: Basic includes 10 GiB of storage with 2 webhooks and 10 GiB total; Standard includes 100 GiB of storage with 10 webhooks; Premium includes 500 GiB of storage with 500 webhooks and adds features like geo-replication, content trust, private link, and customer-managed keys. Additional storage beyond the included amount incurs per-GiB charges across all SKUs.',
  },

  {
    type: 'single-choice',
    id: 'cp-129',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'You want to automatically delete untagged manifests (dangling images) from your Azure Container Registry that are older than 30 days to reclaim storage. Which ACR feature should you use?',
    options: [
      { id: 'a', text: 'Configure an ACR Webhook to trigger an Azure Function that deletes old images' },
      { id: 'b', text: 'Enable ACR retention policies (lifecycle management) on the registry' },
      { id: 'c', text: 'Use ACR geo-replication to purge images from secondary replicas' },
      { id: 'd', text: 'Create an Azure Policy that audits and removes old container images' },
    ],
    correctOptionId: 'b',
    explanation: 'ACR retention policies (available on Premium SKU) allow you to automatically delete untagged manifests after a configurable number of days (1–365). You enable this with "az acr config retention update --registry <name> --status enabled --days 30 --type UntaggedManifests". This is the native, built-in approach for image lifecycle management. Webhooks trigger external processes but are not designed for retention enforcement. Geo-replication is for redundancy, not purging. Azure Policy does not have built-in definitions for managing ACR image lifecycle.',
  },

  {
    type: 'single-choice',
    id: 'cp-130',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Create and manage an Azure container registry',
    stem: 'A company needs to use Azure Container Registry with a private endpoint so that registry traffic never traverses the public internet. After creating the private endpoint, what additional step is required to prevent all public access to the registry?',
    options: [
      { id: 'a', text: 'Delete the registry and recreate it inside the virtual network' },
      { id: 'b', text: 'Disable the registry\'s public network access setting by setting it to "Disabled"' },
      { id: 'c', text: 'Assign the AcrPull role to the private endpoint\'s managed identity' },
      { id: 'd', text: 'Configure a network security group rule blocking port 443 inbound to the registry' },
    ],
    correctOptionId: 'b',
    explanation: 'Creating a private endpoint alone does not disable public access — the registry\'s public endpoint remains accessible by default. To fully restrict access to only private endpoint traffic, you must explicitly disable public network access on the registry (set "publicNetworkAccess" to "Disabled" via portal, CLI, or ARM). This is a two-step process: (1) create the private endpoint, and (2) disable public access. Private endpoints require the Premium SKU. NSGs apply to VNet resources, not directly to PaaS service endpoints. Recreating the registry is unnecessary and destructive.',
  },

  // ─── cp-131 to cp-140: Provision a container by using Azure Container Instances ───

  {
    type: 'single-choice',
    id: 'cp-131',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'You need to deploy a container group in Azure Container Instances that requires a public IP address and must expose both port 80 and port 443. Which network type must you specify?',
    options: [
      { id: 'a', text: 'Private' },
      { id: 'b', text: 'None' },
      { id: 'c', text: 'Public' },
      { id: 'd', text: 'Virtual network integrated' },
    ],
    correctOptionId: 'c',
    explanation: 'Azure Container Instances supports three network types: Public (assigns a public IP, required for internet-facing containers), Private (deploys into a virtual network subnet using VNet integration, gets a private IP only), and None (no network interface, no IP — for batch or processing workloads). For internet-accessible containers on ports 80 and 443, you select Public networking. VNet-integrated containers use the Private type, not a special "Virtual network integrated" type — that is how private networking works.',
  },

  {
    type: 'single-choice',
    id: 'cp-132',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'You deploy an Azure Container Instance from a private Azure Container Registry. The ACI instance fails to pull the image with an authentication error. What is the recommended approach to grant ACI access to the private registry without enabling the registry admin account?',
    options: [
      { id: 'a', text: 'Store registry credentials in Azure Key Vault and reference them in the ACI deployment' },
      { id: 'b', text: 'Assign a system-assigned managed identity to the container group and grant it the AcrPull role on the registry' },
      { id: 'c', text: 'Add the ACI public IP address to the registry\'s firewall IP allow-list' },
      { id: 'd', text: 'Convert the registry SKU to Basic to disable authentication requirements' },
    ],
    correctOptionId: 'b',
    explanation: 'The recommended approach is to assign a managed identity to the ACI container group and grant it the AcrPull role on the registry. This avoids storing credentials and follows the principle of least privilege. The managed identity is specified in the container group\'s "imageRegistryCredentials" using the identity resource ID. Option A (Key Vault) can work for storing service principal secrets but is more complex and still requires a credential. Option C (IP allowlist) addresses network access, not authentication. Option D is incorrect — SKU does not control authentication requirements.',
  },

  {
    type: 'dropdown',
    id: 'cp-133',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'Complete the following statement about Azure Container Instance restart policies.',
    segments: [
      { type: 'text', text: 'For a batch processing job that should run once and stop when the container exits successfully, you should set the restart policy to ' },
      {
        type: 'dropdown',
        id: 'dd1',
        options: ['Always', 'OnFailure', 'Never', 'OnSuccess'],
        correctOption: 'Never',
      },
      { type: 'text', text: '. For a long-running web server container that should automatically restart if it crashes, you should set the restart policy to ' },
      {
        type: 'dropdown',
        id: 'dd2',
        options: ['Always', 'OnFailure', 'Never', 'OnSuccess'],
        correctOption: 'Always',
      },
      { type: 'text', text: '.' },
    ],
    explanation: 'Azure Container Instances supports three restart policies: "Always" (default) — restarts containers whenever they stop, suitable for long-running services; "OnFailure" — restarts containers only when they exit with a non-zero exit code, suitable for batch jobs that might fail; "Never" — containers run at most once and are not restarted regardless of exit code, suitable for one-time tasks. "OnSuccess" is not a valid ACI restart policy. Billing stops when containers stop if restart policy is Never or OnFailure and the container exits successfully.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-134',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'You need to deploy a multi-container solution using Azure Container Instances where a main application container and a sidecar logging container share the same network namespace and filesystem. Which TWO statements are true about ACI container groups? (Select 2)',
    options: [
      { id: 'a', text: 'Containers in the same container group share the same IP address and port space' },
      { id: 'b', text: 'Each container in a group gets its own public IP address' },
      { id: 'c', text: 'Containers in the same group can communicate with each other via localhost' },
      { id: 'd', text: 'Container groups can span multiple Azure regions for high availability' },
      { id: 'e', text: 'All containers in a group are always scheduled on the same host machine' },
    ],
    correctOptionIds: ['a', 'c'],
    requiredSelections: 2,
    explanation: 'An ACI container group is the top-level resource, analogous to a Kubernetes pod. Key characteristics: (A) All containers in a group share a single IP address and port namespace — if two containers both try to use port 80, they conflict; (C) containers communicate with each other using localhost (127.0.0.1) since they share the network namespace. Option B is false — there is one IP per group, not per container. Option D is false — a container group is a single-region resource; multi-region HA requires deploying multiple groups. Option E, while operationally true, is not an exposed guarantee and is not the standard way to describe this — the correct concept is shared lifecycle and network namespace.',
  },

  {
    type: 'single-choice',
    id: 'cp-135',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'You need to mount persistent storage to an Azure Container Instance so that data survives container restarts. Which storage option is natively supported as a volume mount in ACI?',
    options: [
      { id: 'a', text: 'Azure Managed Disk' },
      { id: 'b', text: 'Azure Files (SMB share)' },
      { id: 'c', text: 'Azure Data Lake Storage Gen2' },
      { id: 'd', text: 'Azure Blob Storage (NFS)' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Container Instances natively supports Azure Files shares as volume mounts. You specify the storage account name, storage account key, and share name in the container group definition. The share is mounted via SMB protocol. Azure Managed Disks are supported in AKS and VMs but not natively in ACI. Azure Data Lake Storage Gen2 and Azure Blob NFS are not supported as native ACI volume types. ACI also supports emptyDir (ephemeral, shared within group), gitRepo (deprecated), and secret volumes, but for persistent external storage, Azure Files is the supported choice.',
  },

  {
    type: 'single-choice',
    id: 'cp-136',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'You deploy an Azure Container Instance and need to troubleshoot a startup failure. The container exits immediately after starting. Which command retrieves the container logs to help diagnose the issue?',
    options: [
      { id: 'a', text: 'az aci logs --resource-group myRG --container-name mycontainer' },
      { id: 'b', text: 'az container logs --resource-group myRG --name mycontainergroup' },
      { id: 'c', text: 'az monitor logs --resource mycontainergroup --type ACI' },
      { id: 'd', text: 'az container show --resource-group myRG --name mycontainergroup --query logs' },
    ],
    correctOptionId: 'b',
    explanation: 'The correct command is "az container logs --resource-group myRG --name mycontainergroup". In ACI, the --name parameter refers to the container group name (not the individual container name). If the group has multiple containers, you add "--container-name <name>" to specify which container\'s logs to retrieve. "az aci logs" (option A) is not a valid command namespace. Option C uses "az monitor" which queries Log Analytics, not direct container logs. Option D uses "--query logs" which is not a valid query path in the container group JSON schema.',
  },

  {
    type: 'yes-no',
    id: 'cp-137',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    scenario: 'You are deploying Azure Container Instances for a workload that requires Windows-based containers. Your deployment YAML specifies osType: Windows and a Windows Server Core base image. Review each statement and determine whether it is true.',
    statements: [
      {
        id: 's1',
        text: 'Windows container groups in ACI support the same virtual network integration capabilities as Linux container groups.',
        correct: false,
      },
      {
        id: 's2',
        text: 'Windows container groups in ACI cannot be deployed to all Azure regions that support Linux ACI containers.',
        correct: true,
      },
      {
        id: 's3',
        text: 'A single ACI container group can contain a mix of Windows and Linux containers.',
        correct: false,
      },
    ],
    explanation: 'Windows containers in ACI have several limitations compared to Linux: (s1) Windows container groups have limited VNet integration support — not all networking features available to Linux groups are supported; (s2) Windows ACI availability is region-dependent and not as broadly available as Linux containers; (s3) A container group must have a single OS type — you cannot mix Windows and Linux containers in the same group. The osType property is set at the group level and applies to all containers within it. These restrictions are important to understand when planning Windows workloads on ACI.',
  },

  {
    type: 'single-choice',
    id: 'cp-138',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'You need to pass sensitive configuration values (database passwords and API keys) to an Azure Container Instance at deployment time without storing them in plain text in your ARM template or deployment scripts. What is the recommended approach?',
    options: [
      { id: 'a', text: 'Set the environment variables as "secureValue" type in the container group definition' },
      { id: 'b', text: 'Embed the secrets as base64-encoded values in the container image' },
      { id: 'c', text: 'Store secrets in an Azure Storage Table and have the container read them at startup' },
      { id: 'd', text: 'Pass secrets as command-line arguments in the container\'s "command" array' },
    ],
    correctOptionId: 'a',
    explanation: 'Azure Container Instances supports secure environment variables using the "secureValue" property instead of "value" in the environment variable definition. Secure values are not returned in GET operations or the Azure portal — they are write-only after deployment. This is the purpose-built mechanism for sensitive configuration. Option B (baking secrets into images) is a security anti-pattern — images may be accessible in registries. Option C adds unnecessary complexity and requires the storage account access to be managed. Option D (command-line arguments) are visible in "az container show" output and logs, making them insecure.',
  },

  {
    type: 'drag-drop',
    id: 'cp-139',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'Match each Azure Container Instances deployment scenario to the most appropriate restart policy.',
    items: [
      { id: 'item1', text: 'Never' },
      { id: 'item2', text: 'OnFailure' },
      { id: 'item3', text: 'Always' },
    ],
    targets: [
      { id: 'target1', label: 'A nightly data export job that should run once at 2 AM and stop', correctItemId: 'item1' },
      { id: 'target2', label: 'A web API container that must stay running and recover from crashes', correctItemId: 'item3' },
      { id: 'target3', label: 'A video transcoding job that should retry on errors but stop when complete', correctItemId: 'item2' },
    ],
    explanation: '"Never" is ideal for one-time tasks where you want the container to run exactly once — billing stops when the container exits. "Always" is for persistent services like web APIs where you want automatic restart regardless of exit reason. "OnFailure" suits batch jobs that might encounter transient errors (non-zero exit) and should retry, but should stop without restart when they succeed (exit code 0). Choosing the wrong policy can result in unexpected billing (Always policy on batch jobs) or loss of service (Never policy on web servers).',
  },

  {
    type: 'single-choice',
    id: 'cp-140',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Instances',
    stem: 'You need to deploy a container group to a virtual network so it can communicate with Azure SQL Managed Instance using a private IP. After creating the container group with VNet integration, what resource does ACI automatically create in the subnet?',
    options: [
      { id: 'a', text: 'A network interface (NIC) attached to the container group' },
      { id: 'b', text: 'A network profile that defines the VNet/subnet association' },
      { id: 'c', text: 'A virtual network gateway for routing container traffic' },
      { id: 'd', text: 'A service endpoint for the Microsoft.ContainerInstance service' },
    ],
    correctOptionId: 'b',
    explanation: 'When deploying ACI with VNet integration, Azure creates a "network profile" resource (Microsoft.Network/networkProfiles) in your subscription that stores the VNet and subnet configuration. This network profile is referenced by the container group to attach it to the specified subnet. The subnet must be delegated to Microsoft.ContainerInstance/containerGroups. Note: the network profile resource has been deprecated in newer API versions in favor of specifying the subnet ID directly in the container group definition, but understanding the delegation requirement and the network profile concept is tested on AZ-104.',
  },

  // ─── cp-141 to cp-150: Provision a container by using Azure Container Apps ───

  {
    type: 'single-choice',
    id: 'cp-141',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'You are creating an Azure Container Apps environment. What is the purpose of the Container Apps Environment resource?',
    options: [
      { id: 'a', text: 'It is a virtual machine scale set that runs the underlying container hosts' },
      { id: 'b', text: 'It is a shared boundary that provides networking, logging, and Dapr configuration for a group of container apps' },
      { id: 'c', text: 'It is an Azure Kubernetes Service cluster that runs container apps workloads' },
      { id: 'd', text: 'It is a deployment slot that enables blue-green deployments for container apps' },
    ],
    correctOptionId: 'b',
    explanation: 'An Azure Container Apps Environment is a secure boundary around one or more container apps that provides: shared virtual network integration (optionally), a shared Log Analytics workspace for logs, Dapr configuration and service discovery, and internal load balancing between apps. Multiple container apps within the same environment can communicate with each other using internal service names. The environment runs on a managed infrastructure (which uses AKS internally, but this is abstracted from users). It is not a VM scale set, not an exposed AKS cluster, and not a deployment slot.',
  },

  {
    type: 'single-choice',
    id: 'cp-142',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'You deploy an Azure Container App with HTTP ingress enabled. You want the app to be accessible from the internet. Which ingress setting must you configure?',
    options: [
      { id: 'a', text: 'Set ingress traffic to "Internal only" and create a public IP in the VNet' },
      { id: 'b', text: 'Set ingress traffic to "Accepting traffic from anywhere" (external)' },
      { id: 'c', text: 'Assign a public IP address directly to the container app' },
      { id: 'd', text: 'Configure a NAT gateway on the Container Apps environment subnet' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Container Apps ingress has two traffic settings: "Accepting traffic from anywhere" (external) — makes the app accessible from the public internet via the environment\'s external load balancer with a public IP; "Internal only" — restricts access to within the Container Apps environment and VNet only. To expose an app to the internet, you select the external option. You cannot assign a public IP directly to a container app — ingress is managed by the environment\'s ingress controller. NAT gateways handle outbound traffic, not inbound ingress.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-143',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'You are configuring traffic splitting for an Azure Container App to perform a canary deployment. Which TWO statements correctly describe how Container Apps traffic splitting works? (Select 2)',
    options: [
      { id: 'a', text: 'Traffic is split between different revisions of the container app' },
      { id: 'b', text: 'Traffic is split between different container apps within the same environment' },
      { id: 'c', text: 'Each revision can receive a percentage of traffic, and the percentages must sum to 100' },
      { id: 'd', text: 'Traffic splitting requires configuring an Azure Application Gateway in front of the container app' },
      { id: 'e', text: 'A revision with 0% traffic weight is automatically deactivated and stops incurring costs' },
    ],
    correctOptionIds: ['a', 'c'],
    requiredSelections: 2,
    explanation: 'Azure Container Apps traffic splitting works at the revision level: (A) you assign percentage weights to different revisions of the same container app — enabling canary deployments, A/B testing, or blue-green rollouts; (C) the weights across all active revisions must total exactly 100%. Option B is incorrect — traffic splitting is within a single app across its revisions, not between different apps. Option D is incorrect — Container Apps has built-in ingress and traffic splitting without needing an Application Gateway. Option E is incorrect — a revision with 0% traffic weight is still active (just receives no traffic) unless you explicitly deactivate it.',
  },

  {
    type: 'dropdown',
    id: 'cp-144',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'Complete the following statement about Azure Container Apps revision modes.',
    segments: [
      { type: 'text', text: 'When you want only one revision active at a time and each new deployment automatically deactivates the previous revision, you should configure the revision mode to ' },
      {
        type: 'dropdown',
        id: 'dd1',
        options: ['Single', 'Multiple', 'Latest', 'Active'],
        correctOption: 'Single',
      },
      { type: 'text', text: '. When you want to run multiple revisions simultaneously for A/B testing with traffic splitting, you should configure the revision mode to ' },
      {
        type: 'dropdown',
        id: 'dd2',
        options: ['Single', 'Multiple', 'Latest', 'Active'],
        correctOption: 'Multiple',
      },
      { type: 'text', text: '.' },
    ],
    explanation: 'Azure Container Apps has two revision modes: "Single" mode — only one revision is active at a time; deploying a new version automatically deactivates the previous revision. This is the default and simplest mode for stateless apps. "Multiple" mode — multiple revisions can be active simultaneously, each receiving a defined percentage of traffic. This enables advanced deployment strategies like canary releases and blue-green deployments. "Latest" and "Active" are not valid revision mode values.',
  },

  {
    type: 'single-choice',
    id: 'cp-145',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'A Container App is running with 2 replicas. You need to ensure that the app can authenticate to Azure Key Vault without storing credentials. Which feature should you configure on the Container App?',
    options: [
      { id: 'a', text: 'Configure a secret in the Container App that stores the Key Vault connection string' },
      { id: 'b', text: 'Enable a managed identity on the Container App and grant it Key Vault Secrets User role' },
      { id: 'c', text: 'Store the Key Vault URI as an environment variable in the container revision' },
      { id: 'd', text: 'Use Dapr secret store component with the container app\'s registry credentials' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Container Apps supports both system-assigned and user-assigned managed identities. Enabling a managed identity and granting it the "Key Vault Secrets User" (or "Key Vault Secrets Reader") role on the Key Vault allows the container app to authenticate to Key Vault without any credentials — using the Azure identity platform. This is the recommended, credential-free approach. Option A (Container App secrets) stores static values but still requires a credential to access Key Vault. Option C (environment variables) only stores the URI, not the authentication credential. Option D (Dapr) can work with Key Vault via a secret store component but requires additional configuration and does not eliminate credentials by itself.',
  },

  {
    type: 'yes-no',
    id: 'cp-146',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    scenario: 'You are deploying a microservices application using Azure Container Apps. You plan to use Dapr (Distributed Application Runtime) integration. Review each statement about Dapr in Azure Container Apps and determine whether it is true.',
    statements: [
      {
        id: 's1',
        text: 'Enabling Dapr on a Container App automatically injects a Dapr sidecar container into the container app replica.',
        correct: true,
      },
      {
        id: 's2',
        text: 'Container Apps using Dapr can only communicate with other Container Apps in the same environment, not with external Dapr-enabled services.',
        correct: false,
      },
      {
        id: 's3',
        text: 'Each Container App must have a unique Dapr application ID (app-id) within the Container Apps environment for service discovery to work.',
        correct: true,
      },
    ],
    explanation: '(s1) True — when you enable Dapr on a Container App, Azure automatically injects a Dapr sidecar container alongside the application container in each replica. You configure the Dapr app port and app ID. (s2) False — Dapr in Container Apps can communicate with external services; Dapr components (pub/sub, state stores, bindings) can reference external Azure services like Service Bus, Cosmos DB, and Blob Storage. Dapr service invocation works within the environment via the Dapr name resolution component. (s3) True — the Dapr app ID is the service identity used for service invocation, pub/sub, and tracing. Duplicate app IDs in the same environment would cause routing conflicts.',
  },

  {
    type: 'single-choice',
    id: 'cp-147',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'You deploy an Azure Container App that processes messages from an Azure Service Bus queue. You want the app to scale based on the number of messages in the queue. Which scaling trigger type should you configure?',
    options: [
      { id: 'a', text: 'HTTP scaling rule based on concurrent requests' },
      { id: 'b', text: 'Azure Service Bus scaling rule (KEDA scaler)' },
      { id: 'c', text: 'CPU-based scaling rule with a 70% threshold' },
      { id: 'd', text: 'Memory-based scaling rule with an 80% threshold' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Container Apps scaling is powered by KEDA (Kubernetes-based Event Driven Autoscaling). For queue-based workloads, you configure an event-driven scaling rule using the Azure Service Bus scaler. This allows the number of replicas to scale from 0 to N based on the message count in the queue or topic subscription. HTTP scaling (option A) is for request-based web workloads. CPU and memory scaling (options C and D) are resource-based and do not directly reflect queue depth — they are reactive rather than proactive. Event-driven scaling is the purpose-built approach for message processing workloads and can scale to zero when the queue is empty, reducing costs.',
  },

  {
    type: 'single-choice',
    id: 'cp-148',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'You need to configure a Container App to securely reference a secret stored in Azure Key Vault so that the secret value is available as an environment variable inside the container without storing the value in the Container App configuration. Which approach enables this?',
    options: [
      { id: 'a', text: 'Reference the Key Vault secret URI directly in the environment variable value field' },
      { id: 'b', text: 'Create a Container App secret that references the Key Vault secret URI using the managed identity, then reference the Container App secret as an environment variable' },
      { id: 'c', text: 'Mount the Key Vault secret as a volume and read the file from within the container' },
      { id: 'd', text: 'Configure a Dapr secret store component and call the Dapr API from within the container' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Container Apps supports Key Vault secret references in its secret configuration. You create a Container App secret with the "keyVaultUrl" pointing to the Key Vault secret URI and specify the managed identity to use for access. Then you reference this Container App secret as an environment variable using "secretRef". This approach keeps the actual secret value out of the Container App configuration (it is fetched from Key Vault at runtime) while making it available as an environment variable. Option A does not work — direct Key Vault URIs in environment variable values are not automatically resolved. Option C requires additional in-container code. Option D (Dapr) requires API calls in application code.',
  },

  {
    type: 'drag-drop',
    id: 'cp-149',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'You are designing the ingress configuration for multiple Azure Container Apps in the same environment. Match each ingress scenario to the correct ingress accessibility level.',
    items: [
      { id: 'item1', text: 'External (accessible from internet)' },
      { id: 'item2', text: 'Internal (environment only)' },
      { id: 'item3', text: 'No ingress (no HTTP access)' },
    ],
    targets: [
      { id: 'target1', label: 'A public-facing REST API consumed by mobile apps', correctItemId: 'item1' },
      { id: 'target2', label: 'A background worker that processes queue messages with no HTTP endpoints', correctItemId: 'item3' },
      { id: 'target3', label: 'An internal authentication microservice called only by other container apps in the same environment', correctItemId: 'item2' },
    ],
    explanation: 'Container Apps ingress accessibility levels: External — app receives traffic from the public internet via the environment\'s public IP/domain; required for customer-facing APIs and web apps. Internal — app is accessible only within the Container Apps environment or connected VNet, not from the internet; ideal for microservices in a backend tier. No ingress — HTTP ingress is disabled entirely; the app runs but cannot receive HTTP traffic; appropriate for workers, processors, and jobs that only consume from queues or perform scheduled tasks. Choosing the wrong level can expose internal services publicly or prevent legitimate traffic.',
  },

  {
    type: 'single-choice',
    id: 'cp-150',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Provision a container by using Azure Container Apps',
    stem: 'You want to run a short-lived, one-time data migration task using Azure Container Apps. The task should run once, perform the migration, and stop. Which Container Apps feature is designed for this use case?',
    options: [
      { id: 'a', text: 'Configure a revision with minReplicas: 0 and maxReplicas: 1' },
      { id: 'b', text: 'Use Azure Container Apps Jobs' },
      { id: 'c', text: 'Deploy the app with restart policy set to Never' },
      { id: 'd', text: 'Configure a KEDA schedule-based scaling rule to scale to 0 after completion' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Container Apps Jobs are purpose-built for finite, task-oriented workloads. A Job runs one or more containers to completion and exits — ideal for data migrations, report generation, image processing, and CI/CD pipeline tasks. Jobs support manual triggers, scheduled triggers (cron), and event-driven triggers. Unlike container apps (which are for long-running services), jobs are not expected to run continuously. Option A describes scaling a service to zero replicas, not a one-time task. Option C — Container Apps does not have a "restart policy Never" setting (that is an ACI concept). Option D uses KEDA for scaling, not for one-time task execution.',
  },

  // ─── cp-151 to cp-160: Manage sizing and scaling for containers ───

  {
    type: 'single-choice',
    id: 'cp-151',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'You are configuring resource requests for an Azure Container Instance. The container needs at least 2 CPU cores and 4 GiB of memory to function properly. What is the correct Azure CLI command to deploy the container with these resource specifications?',
    options: [
      { id: 'a', text: 'az container create --cpu 2 --memory 4 --resource-group myRG --name mycontainer --image myimage' },
      { id: 'b', text: 'az container create --vcpu-count 2 --memory-gb 4 --resource-group myRG --name mycontainer --image myimage' },
      { id: 'c', text: 'az container create --cpu-cores 2 --memory-gib 4 --resource-group myRG --name mycontainer --image myimage' },
      { id: 'd', text: 'az container create --compute 2cpu-4gb --resource-group myRG --name mycontainer --image myimage' },
    ],
    correctOptionId: 'a',
    explanation: 'The correct Azure CLI parameters for ACI resource specifications are "--cpu" (number of CPU cores, decimal values like 0.5 are supported) and "--memory" (memory in GiB, decimal values supported). So "--cpu 2 --memory 4" specifies 2 CPU cores and 4 GiB RAM. The other options use incorrect parameter names: "--vcpu-count", "--memory-gb", "--cpu-cores", "--memory-gib", and "--compute" are not valid Azure CLI parameters for "az container create". ACI supports CPU from 0.1 to 4 cores and memory from 0.1 to 16 GiB for Linux containers (lower limits for Windows).',
  },

  {
    type: 'single-choice',
    id: 'cp-152',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'An Azure Container App is experiencing high latency during traffic spikes. The app is currently configured with minReplicas: 1 and maxReplicas: 3, and HTTP scaling is enabled. During peak load, you observe that the app maxes out at 3 replicas but latency remains high. What is the most appropriate action?',
    options: [
      { id: 'a', text: 'Increase the container\'s CPU and memory resource limits in the revision configuration' },
      { id: 'b', text: 'Increase maxReplicas to a higher value to allow more instances to handle traffic' },
      { id: 'c', text: 'Switch the Container Apps environment from Consumption plan to Dedicated plan' },
      { id: 'd', text: 'Enable Dapr on the container app to distribute the request load' },
    ],
    correctOptionId: 'b',
    explanation: 'If the container app is already at its maximum replica count (3) and latency is still high, the scaling ceiling is the bottleneck. Increasing maxReplicas allows the autoscaler to provision more replicas to handle additional load. Option A (increasing CPU/memory per replica) addresses per-instance resource constraints but does not help if the issue is concurrent request throughput requiring more instances. Option C (switching to Dedicated plan) changes the underlying compute but does not directly solve a maxReplicas ceiling issue. Option D (Dapr) adds observability and service mesh capabilities but does not distribute load in a way that compensates for insufficient replica count.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-153',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'You are configuring autoscaling for an Azure Container App. Which THREE scaling trigger types are natively supported by Azure Container Apps? (Select 3)',
    options: [
      { id: 'a', text: 'HTTP concurrent requests per replica' },
      { id: 'b', text: 'Azure Storage Queue message count' },
      { id: 'c', text: 'CPU utilization percentage' },
      { id: 'd', text: 'Scheduled time-based (cron) scaling' },
      { id: 'e', text: 'Number of active database connections' },
    ],
    correctOptionIds: ['a', 'b', 'c'],
    requiredSelections: 3,
    explanation: 'Azure Container Apps supports multiple scaling trigger types powered by KEDA: (A) HTTP — scales based on concurrent HTTP requests per replica, with a configurable threshold; (B) Azure Queue — scales based on the number of messages in Azure Storage Queue or Azure Service Bus queue; (C) CPU and Memory — resource-based scaling on CPU utilization or memory usage percentage. Additional supported scalers include Azure Event Hubs, Apache Kafka, custom KEDA scalers, and more. Option D (scheduled/cron scaling) is not a native Container Apps scaling rule type — though KEDA has a cron scaler, it is used for Jobs rather than apps. Option E (database connections) is not a built-in scaler without a custom KEDA configuration.',
  },

  {
    type: 'dropdown',
    id: 'cp-154',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'Complete the following statement about Azure Container Apps scaling behavior.',
    segments: [
      { type: 'text', text: 'When an Azure Container App is configured with minReplicas set to ' },
      {
        type: 'dropdown',
        id: 'dd1',
        options: ['0', '1', '2', '3'],
        correctOption: '0',
      },
      { type: 'text', text: ', the app can scale to zero replicas when there is no traffic, which results in ' },
      {
        type: 'dropdown',
        id: 'dd2',
        options: ['higher cost due to reserved capacity', 'no charges for compute when idle', 'automatic deletion of the container app', 'failover to another region'],
        correctOption: 'no charges for compute when idle',
      },
      { type: 'text', text: ' under the Consumption plan.' },
    ],
    explanation: 'Setting minReplicas to 0 enables "scale to zero" behavior — when there are no incoming requests or events, Container Apps scales down to zero replicas. Under the Consumption plan, you are charged only for resources consumed while replicas are running; when scaled to zero, there are no compute charges (you pay only for requests when they come in). This is a major cost optimization for intermittent workloads. Note that scale-to-zero introduces cold start latency when the first request arrives after a period of inactivity. Setting minReplicas to 1 or more prevents scale-to-zero but eliminates cold start.',
  },

  {
    type: 'single-choice',
    id: 'cp-155',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'You have an Azure Container App with HTTP scaling configured. The current scaling rule is set to scale out when concurrent requests exceed 10 per replica. The app has maxReplicas: 10. During a load test, you observe that at 90 concurrent requests the app has 9 replicas but is not scaling to 10. What is the most likely cause?',
    options: [
      { id: 'a', text: 'The HTTP scaling rule threshold of 10 requires exactly 100 concurrent requests to trigger a 10th replica' },
      { id: 'b', text: 'The Container Apps environment has reached its regional quota limit' },
      { id: 'c', text: 'Scale-out operations have a cooldown period that prevents immediate scaling to the maximum' },
      { id: 'd', text: '90 requests with a threshold of 10 per replica requires only 9 replicas (90/10 = 9), which is correct behavior' },
    ],
    correctOptionId: 'd',
    explanation: 'This is correct and expected behavior. With an HTTP scaling threshold of 10 concurrent requests per replica and 90 active concurrent requests, the autoscaler calculates: 90 requests ÷ 10 per replica = 9 replicas needed. The app would only scale to a 10th replica when concurrent requests exceed 90 (i.e., 91+). This is how KEDA-based autoscaling works — it divides the current metric value by the threshold to determine the desired replica count. Option A is mathematically incorrect. Option B (quota limits) would cause scaling to stop at a lower number due to hard limits, not exactly at 9. Option C (cooldown) would cause a delay, not a permanent ceiling at 9.',
  },

  {
    type: 'yes-no',
    id: 'cp-156',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'You are managing an Azure Container Registry with a Standard SKU and have deployed container workloads to both Azure Container Instances and Azure Container Apps. Review each statement about container sizing and resource management and determine whether it is true.',
    statements: [
      {
        id: 's1',
        text: 'In Azure Container Instances, you can change the CPU and memory allocated to a running container without stopping and redeploying the container group.',
        correct: false,
      },
      {
        id: 's2',
        text: 'In Azure Container Apps, updating the container resource limits (CPU/memory) creates a new revision of the container app.',
        correct: true,
      },
      {
        id: 's3',
        text: 'Azure Container Apps on the Consumption plan supports a maximum of 4 vCPUs and 8 GiB of memory per replica.',
        correct: true,
      },
    ],
    explanation: '(s1) False — Azure Container Instances does not support live resizing. To change CPU or memory, you must delete and redeploy the container group with new resource specifications. This is a significant operational consideration. (s2) True — In Azure Container Apps, any change to the container configuration (including resource limits) that creates a new revision results in a new immutable revision being created. If in Single revision mode, the new revision replaces the old one; in Multiple mode, both can coexist. (s3) True — The Consumption plan limits per replica to a maximum of 4 vCPUs and 8 GiB of memory. The Dedicated plan (using dedicated workload profiles) supports larger sizes. Valid CPU/memory combinations must follow the 1:2 ratio rule (e.g., 1 CPU = up to 2 GiB RAM).',
  },

  {
    type: 'single-choice',
    id: 'cp-157',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'You are deploying a Container App that requires GPU resources for ML inference workloads. Which Azure Container Apps plan supports GPU-enabled workload profiles?',
    options: [
      { id: 'a', text: 'Consumption plan' },
      { id: 'b', text: 'Dedicated plan with GPU-enabled workload profiles' },
      { id: 'c', text: 'Premium plan' },
      { id: 'd', text: 'GPU workloads are not supported in Azure Container Apps; use Azure Container Instances instead' },
    ],
    correctOptionId: 'b',
    explanation: 'Azure Container Apps Dedicated plan supports workload profiles, which are pre-defined VM types allocated to a Container Apps environment. GPU-enabled workload profiles (using NC-series or NV-series VMs) are available in the Dedicated plan, allowing GPU-accelerated workloads for ML inference, video processing, and scientific computing. The Consumption plan does not support GPU resources — it uses shared, serverless compute with CPU and memory only. There is no "Premium plan" for Container Apps (that is an App Service concept). ACI does support GPU SKUs, but Container Apps Dedicated plan also supports GPUs.',
  },

  {
    type: 'drag-drop',
    id: 'cp-158',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'Match each container scaling scenario to the most appropriate Azure service or feature.',
    items: [
      { id: 'item1', text: 'Azure Container Apps with KEDA event-driven scaling' },
      { id: 'item2', text: 'Azure Container Instances (manual sizing, no autoscaling)' },
      { id: 'item3', text: 'Azure Container Apps with HTTP scaling and minReplicas: 0' },
    ],
    targets: [
      { id: 'target1', label: 'A stateless REST API with unpredictable traffic that must scale to zero during off-hours to minimize cost', correctItemId: 'item3' },
      { id: 'target2', label: 'A one-time database migration script that requires 4 CPU cores and 8 GiB RAM for approximately 30 minutes', correctItemId: 'item2' },
      { id: 'target3', label: 'A message processor that scales from 0 to 50 replicas based on Azure Service Bus queue depth', correctItemId: 'item1' },
    ],
    explanation: 'Matching rationale: (target1) A serverless REST API with variable traffic benefits from Container Apps HTTP scaling with scale-to-zero (minReplicas: 0) — cost is minimized when idle, and the app automatically scales with demand. (target2) A one-time fixed-resource task with known requirements is best suited to ACI, which offers direct sizing control and a simple deploy-run-delete lifecycle without scaling complexity. (target3) Queue-depth-based scaling is the canonical use case for KEDA event-driven scaling in Container Apps, which can scale from 0 to maxReplicas based on the message count in a Service Bus queue.',
  },

  {
    type: 'single-choice',
    id: 'cp-159',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'You have an Azure Container App configured with the following scaling rule: minReplicas: 2, maxReplicas: 20, HTTP scaling threshold: 100 concurrent requests per replica. The app receives 1500 concurrent requests. How many replicas will the autoscaler target?',
    options: [
      { id: 'a', text: '10 replicas' },
      { id: 'b', text: '15 replicas' },
      { id: 'c', text: '20 replicas (capped at maxReplicas)' },
      { id: 'd', text: '2 replicas (stays at minimum)' },
    ],
    correctOptionId: 'b',
    explanation: 'The autoscaler calculates: 1500 concurrent requests ÷ 100 per replica = 15 replicas. Since 15 is within the range of minReplicas (2) and maxReplicas (20), the target is exactly 15 replicas. The scaling is not capped because 15 < 20. If the calculation yielded more than 20, it would be capped at 20 (the maxReplicas). If it yielded fewer than 2, the minimum of 2 would be enforced. Understanding this arithmetic is important for capacity planning and cost estimation for Container Apps workloads.',
  },

  {
    type: 'multiple-choice',
    id: 'cp-160',
    sectionId: 'compute',
    subsectionId: 'containers',
    bulletPoint: 'Manage sizing and scaling for containers',
    stem: 'You are reviewing the compute options for containerized workloads. Which TWO statements correctly describe a key difference between Azure Container Instances and Azure Container Apps with respect to scaling? (Select 2)',
    options: [
      { id: 'a', text: 'Azure Container Instances does not support automatic horizontal scaling; you must manage the number of container group instances manually or through external orchestration' },
      { id: 'b', text: 'Azure Container Apps supports automatic horizontal scaling based on HTTP traffic, queue length, and custom KEDA scalers' },
      { id: 'c', text: 'Azure Container Instances supports automatic vertical scaling, adjusting CPU and memory without redeployment' },
      { id: 'd', text: 'Azure Container Apps can only scale based on CPU and memory metrics, not event-driven triggers' },
      { id: 'e', text: 'Both Azure Container Instances and Azure Container Apps support automatic scale-to-zero when idle' },
    ],
    correctOptionIds: ['a', 'b'],
    requiredSelections: 2,
    explanation: '(A) True — ACI has no built-in autoscaler. Each container group is a fixed unit. To scale horizontally, you deploy multiple container groups via Azure Logic Apps, Azure Functions, or ARM templates triggered by external logic. (B) True — Container Apps has KEDA-based autoscaling built in, supporting HTTP traffic, Azure Queue/Service Bus message counts, CPU/memory, and any custom KEDA scaler. (C) False — ACI does not support live vertical scaling; CPU and memory changes require redeployment. (D) False — Container Apps supports event-driven triggers (queue length, etc.), not just resource metrics. (E) False — ACI does not support scale-to-zero automatically; containers must be explicitly deleted to stop billing. Container Apps supports scale-to-zero with minReplicas: 0.',
  },
];
